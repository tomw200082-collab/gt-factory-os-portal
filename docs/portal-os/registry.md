# Portal OS Registry

Index of every operating artifact. Entries use repo-root-relative paths so the PR guard's registry presence check (`grep -Fq` on path) passes on every new artifact.

## Commands
- `.claude/commands/portal-audit.md` — deep admin audit, dispatches three auditors in parallel
- `.claude/commands/portal-readiness.md` — consolidated headline: scorecard + audits + tranches + drift
- `.claude/commands/portal-regression-guard.md` — dispatch regression-sentinel; fail on baseline drift
- `.claude/commands/portal-scorecard.md` — recompute 10-category readiness JSON + markdown mirror
- `.claude/commands/portal-tranche-fix.md` — execute tranche NNN as ONE bounded commit set with verification
- `.claude/commands/portal-tranche-plan.md` — propose next tranche from top scorecard gap

## Subagents
- `.claude/agents/portal-admin-surface-auditor.md` — admin-as-superuser depth audit
- `.claude/agents/portal-flow-continuity-auditor.md` — end-to-end journey walkability
- `.claude/agents/portal-regression-sentinel.md` — baseline + quarantine drift detector
- `.claude/agents/portal-route-auditor.md` — structural route + nav surface audit
- `.claude/agents/portal-tranche-verifier.md` — post-fix verification gate

## Hooks
- `.claude/hooks/pre_tool_use.sh` — tranche manifest + quarantine + secrets structural backstop
- `.claude/hooks/session_start.sh` — scorecard + active tranche + drift opening context
- `.claude/hooks/stop.sh` — no-dead-air: requires a Next action: line
- `.claude/hooks/subagent_stop.sh` — PASS/complete claims require an Evidence: path

## Workflows
- `.github/workflows/claude.yml` — @claude mention handler (mobile-primary entry)
- `.github/workflows/portal-drift-weekly.yml` — weekly drift + readiness; opens issue on regression
- `.github/workflows/portal-pr-guard.yml` — typecheck + registry presence on every PR

## Canonical artifacts
- `docs/portal-os/baseline.json` — frozen repo-truth snapshot; regression-sentinel compares against it
- `docs/portal-os/quarantine.json` — dead/fake/quarantined path list + forbidden_strings
- `docs/portal-os/route-manifest.json` — canonical list of live routes, roles, status
- `docs/portal-os/scorecard.json` — 10-category readiness score (machine-readable)
- `docs/portal-os/scorecard.md` — human-readable mirror of scorecard.json
- `docs/portal-os/dashboard-target-design.md` — dashboard design constitution v2 ("Factory Flow Instrument"): 3 design laws, Focus Engine spec, Flow Ribbon node table, Today's Work queue, Week panel, color/motion/performance budgets, phase convergence map

## Migration docs
- `docs/portal-os/MIGRATION-HISTORY.md` — history of OS merges onto main (what was imported, what was kept, rationale)

## Background packages
- `docs/portal-os/backend-package-admin-superuser-depth.md` — 5-deliverable backend package to move admin_superuser_depth to 10/10

## Audit reports & plans
- `docs/portal-os/audit-reports/2026-06-11-full-system.md` — 10-investigation full-system audit (routes, admin, flows, design, interactions, taxonomy, prices, planning paths, production reporting, PO creation)
- `docs/portal-os/audit-reports/2026-06-12-dashboard-ui-audit.md` — dashboard UI/visual audit: 8 P0 trust findings, 6 IA findings, 7 visual-system findings, 4-phase plan (truth → hierarchy → calm → structure)
- `docs/portal-os/improvement-plan-2026-06.md` — proposed 7-phase master improvement plan derived from the 2026-06-11 audit; pending Tom decisions T1–T6

## Tranches (history + state)
- `docs/portal-os/tranches/000-template.md` — tranche template (do not set status=proposed on this file)
- `docs/portal-os/tranches/_active.txt` — contains the currently active tranche number, or empty
- `docs/portal-os/tranches/001-bootstrap-truthfulness.md` — (PR #3 branch history) truthfulness of OS files vs code
- `docs/portal-os/tranches/002-fake-auth-identifier-rename.md` — (PR #3 branch history) FakeSession → DevShimSession rename
- `docs/portal-os/tranches/003-role-gate-manifest-alignment.md` — (PR #3 branch history) manifest pinned to layouts
- `docs/portal-os/tranches/004-physical-count-cancel-wire.md` — (PR #3 branch history) stop leaking server snapshots
- `docs/portal-os/tranches/005-planning-ux-polish.md` — (PR #3 branch history) cache + link hygiene
- `docs/portal-os/tranches/006-ops-banner-deep-links.md` — (PR #3 branch history) pending approvals clickable
- `docs/portal-os/tranches/007-stale-e2e-cleanup.md` — (PR #3 branch history) delete obsolete goods-receipt spec
- `docs/portal-os/tranches/008-mobile-parity.md` — mobile/tablet experience (MobileNav + TopBar + AppShellChrome)
- `docs/portal-os/tranches/009-error-boundaries.md` — error.tsx + global-error.tsx + obs/report.ts
- `docs/portal-os/tranches/010-dashboard-live-kpis.md` — KpiTiles component (three live counts above the fold)
- `docs/portal-os/tranches/011-security-hardening.md` — env.ts header + env validation
- `docs/portal-os/tranches/012-po-detail-page.md` — (superseded by main's Tranche D; plan preserved as history)
- `docs/portal-os/tranches/013-receipt-po-linkage.md` — (PR #3 branch history) close PO chain break
- `docs/portal-os/tranches/014-inbox-federation.md` — (superseded by main's Tranche B 4-stream federation)
- `docs/portal-os/tranches/015-role-boundary-e2e.md` — role-boundaries.spec.ts 12-test matrix
- `docs/portal-os/tranches/016-middleware-role-gates-and-manifest-completeness.md` — middleware scaffold for layer 3 role gating
- `docs/portal-os/tranches/017-deploy-unblock-dashboard-dynamic.md` — (superseded by main's Tranche C making dashboard "use client")
- `docs/portal-os/tranches/018-public-landing-preview-resilience.md` — public landing at / resilient to auth failures
- `docs/portal-os/tranches/019-bulletproof-root.md` — hardened root page (static, middleware-bypass)
- `docs/portal-os/tranches/020-economics-page-ux-polish.md` — Economics page filters + cost-gaps drawer + publish affordance + UX sweep
- `docs/portal-os/tranches/021-receipt-smart-picker.md` — Smart Picker + PO Ledger + per-line progress pills for /stock/receipts
- `docs/portal-os/tranches/022-format-qty-sweep.md` — fmtNumStr() applied portal-wide to strip 8-dp noise from qty displays
- `docs/portal-os/tranches/023-ux-pro-polish.md` — replace emoji decorations with Lucide icons across receipts surface
- `docs/portal-os/tranches/024-operator-forms-design-pass.md` — operator forms: hero numerics, typography, primary CTA, step-indicator
- `docs/portal-os/tranches/025-physical-count-pro-redesign.md` — physical-count: pro-grade redesign, unified banner, cancel polish
- `docs/portal-os/tranches/026-economics-pl-coverage.md` — Economics Overview: P&L Coverage frame (dual-axis revenue+SKU), demand-weighted sort, chip taxonomy split, honest inventory totals
- `docs/portal-os/tranches/027-procurement-shared-line-editor.md` — extract mode-aware PoLineEditor + useOrderables from /new (pure refactor); foundation for the procurement-merge epic (028–030: unified action-list page, focus mode + inline create, ad-hoc in session)
- `docs/portal-os/tranches/028-procurement-unified-action-list.md` — new /planning/procurement merged page; default view = action list grouped by decision (must-today / can-wait / handled) with a derived "why now"; nav swaps Purchase Session + Calendar for one Procurement entry; entry point for focus mode (029)
- `docs/portal-os/tranches/029-procurement-focus-mode.md` — full-screen one-order-at-a-time focus overlay (approve→place→next, auto-advance, keyboard, order document) reusing the session mutations; launched from the 028 action list
- `docs/portal-os/tranches/030-procurement-adhoc-in-session.md` — ad-hoc add-a-line inside focus mode via the shared useOrderables picker + add_lines; one-off new-supplier order routes to the manual PO form; closes the procurement-merge epic
- `docs/portal-os/tranches/031-procurement-focus-hardening.md` — post-merge hardening of the focus mode: mobile table scroll, dialog focus-trap + initial focus, mobile footer, remaining-aware completion screen (+ remainingCount helper/test)
- `docs/portal-os/tranches/032-procurement-test-coverage.md` — direct ActionList + FocusMode integration tests (controller/grouping), Hebrew day-grammar in "why now", progressbar a11y; triage of the 35 pre-existing unrelated failures
- `docs/portal-os/tranches/033-procurement-calendar-view.md` — folds the calendar timeline into /planning/procurement as a secondary view (toggle: action list / calendar), derived from session.pos with a pure tested grid engine; day chips open focus mode
- `docs/portal-os/tranches/034-recipe-health-test-repair.md` — repairs 7 stale recipe-health-card assertions (label-in-two-places idiom + `{row}` clone envelope); flags 1 genuine draft-exists-modal discrepancy as FIXME; full suite 35→28 fails (phase 1 toward green + CI-gated vitest)
- `docs/portal-os/tranches/035-test-suite-green-sweep.md` — drives the whole unit suite red→green (35 fails → 0; 30 stale-assertion fixes + 5 documented obsolete-doctrine skips) and wires `vitest` into portal-pr-guard so the suite can't silently rot again
- `docs/portal-os/tranches/036-focus-e2e-doctrine-ci.md` — route-mocked focus-mode e2e (approve→place→done), re-anchors the items-bom doctrine to the products surface (0 skips), and gates the `@mocked` e2e in CI (chromium + dev-shim, no backend)
- `docs/portal-os/tranches/037-weekly-meeting-ux-a11y.md` — accessibility + interaction pass over the `/planning/meeting` cockpit (cadence-rail step semantics, live-region announcements, aria-busy, labelled day groups, disabled-reason, confirm-focus); +9 meeting-a11y unit tests; suite 395→404
- `docs/portal-os/tranches/038-login-split-panel-design.md` — `/login` brought to the Design System "Operational Precision" split-panel layout (dark brand-hero left + existing sign-in card right, hero `hidden lg:flex`); presentation-only re-wrap, zero auth-logic change, all 16 login data-testids preserved
- `docs/portal-os/tranches/039-reconcile-badge-lucide-icon.md` — ReconcileBadge's leading `⚠` unicode glyph swapped for the Lucide `<AlertTriangle>` (design-system "Lucide-only icons, never emoji/unicode" rule); presentation-only, zero logic change, full suite 416/416 green
- `docs/portal-os/tranches/039-dashboard-data-viz.md` — `/dashboard` time-series band: Production activity (SVG area+line) + Stock movement flow (grouped bars) + indicative RM+PKG Inventory value (reconstructed, coverage-aware) with interactive crosshair/tooltip + shared 7/14/30-day range selector; honest UOM-agnostic counts, no new backend; pure tested `_lib/trends.ts` + `_lib/value-trend.ts` helpers
- `docs/portal-os/tranches/047-procurement-po-interaction-pack.md` — Procurement/PO interaction pack (Phase 6 portal-only): D1 supplier comparison strip on PO lines (radio chips: name · ₪cost-per-order-UOM via std_cost × pack_conversion · lead days · MOQ; primary pre-selected; chip pins `supplier_item_id`, sent only when it matches the header supplier) + per-line "no mapping — submitting will fail" warning + MOQ hint under qty + catalog price placeholder, new `useSupplierItemsByOrderable` hook (cached per orderable via `/api/supplier-items`), D2 expected-date default = today + max line lead time (falls back to supplier `default_lead_time_days`, then 7; "based on X-day lead time" helper; user-touched dates never overridden), INTER-006 PO-list skeleton KPI tiles + INTER-008 500-row truncation banner, INTER-007 inventory-flow "Clear all" filter reset, INTER-013 PublishConfirmModal `isSubmitting` (both buttons disabled + spinner, wired to publish `isPending`), 045 follow-ups: dashboard urgent-procurement links → `/planning/procurement` + procurement empty-session calendar-link loop removed; 13 new tests, suite green
- `docs/portal-os/tranches/048-production-board-pack.md` — Production board & reporting pack (Phase 6 portal-only tier): C6 one-tap "Confirm: produced N exactly as planned" fast path on `/stock/production-actual` (from_plan_id + untouched qty), C7 Tier 1 "Re-plan remainder for tomorrow" on under-plan success (POSTs a linked plan row via the existing create endpoint) + commit-time `committedPlan` capture fixing the previously-unreachable success variance row, INTER-004 ManualAddModal UoM select (contract UOMS + row uoms) + inline 422 field errors, INTER-011 60s refetch + manual Refresh + "Updated HH:MM", INTER-012 disabled-reason titles, INTER-005 filled btn-danger cancel confirm, INTER-010 32px touch targets, D13 Tier 1 Today strip (planned/reported/unreported + tomorrow preview + "Move to tomorrow"); pure tested `_lib/board-summary.ts`; 465/465 green (451 + 14 new)
- `docs/portal-os/tranches/046-os-truthfulness-role-gates.md` — OS truthfulness + role-gate reconciliation (Phase 5): route-manifest regenerated to cover every `src/app/**/page.tsx` (41 → 77 rows; roles derived from actual layout gates + lattice; T6: users/jobs/integrations → live; redirect stubs marked), baseline.json anchors populated (routes/nav_items/role_gates) + invariants corrected, quarantine.json seeded with the forecast-spec pending-cleanup vestige, middleware ROLE_GATES reconciled with layouts (movement-log + economics carve-outs, planner in /stock, all-roles `/planning` + `/purchase-orders` + `/exceptions`), integrations cards say "derived from exception activity — not sync telemetry", jobs staleness marked "(estimated from job name)", BOM editor "Discard changes" → "Close editor" truth copy; 451/451 green
- `docs/portal-os/tranches/045-planning-consolidation.md` — Planning consolidation (one declared workflow): "Run History" removed from primary nav + planning sub-nav (pages kept; diagnostic-only banners on `/planning/runs` + `[run_id]`), purchase-session/purchase-calendar → redirect stubs to `/planning/procurement`, weekly-outlook → redirect to `/planning/inventory-flow` (`_lib` dirs kept), meeting PROCURE tiles re-pointed to Procurement, hub cadence block "How planning works here" (Thursday/Sunday/Daily), route-manifest rows → status `redirect`; 451/451 green
- `docs/portal-os/tranches/044-groups-v1-portal.md` — Groups v1 portal: shared taxonomy module (`src/lib/taxonomy/groups.ts`) + `GroupFilterBar`, 4 groups proxies, /inventory regex-taxonomy → real `product_group_key`/`material_group_key` with honest "ללא קבוצה" bucket + RM/PKG "לפי קו מוצר" row, FG-flow `?product_group=` chips, supply-flow `?material_group=`/`?used_by_product_group=` chips, /admin/items group filter + inline assign, new `/admin/groups` management page + nav entry; 451/451 green (435 + 16 new)
- `docs/portal-os/tranches/043-po-price-entry-cost-drafts.md` — Price Truth portal close: optional per-line unit price on `/purchase-orders/new` + focus-mode place (`line_prices` + `confirm_price_update`, default-checked, shown only when a price was entered), 3 cost-drafts proxies, `/admin/cost-drafts` "Price updates" review queue (delta-% badges at the 25% threshold, PO source links, approve/reject with stock-value + economics invalidation), admin nav entry; 435/435 green
- `docs/portal-os/tranches/050-production-backend-wiring.md` — Phase 6 close: submission detail view, reversal UI, variance reasons, availability columns, draft/base-batch board states
- `docs/portal-os/tranches/072-overnight-ux-precision.md` — Overnight UX precision pass (Tom 2026-06-15, autonomous; PR #95 draft). 12 waves, all `tsc`+vitest green (710 tests), portal-only, backend-blocked skipped. **What-to-order decision support (Tom's redirected priority):** decode the per-line `coverage_trace` the session API returned but the portal discarded → pure `coverage-trace.ts` (parse + `buildCoverageReasoning`, stockout/below_safety/ok); render "why this quantity" (demand/on-hand/incoming/projected-at-need) in focus mode + ActionList scan view; surface `session.warnings`; need-by/order-by date pair on rows; material-requirements card reads as Required − On hand = To order. **Price/cost accuracy:** `computeLinePriceInsight` per-line total + variance-vs-catalog + catalog-writeback caution; PO detail cost summary. **Every-button precision:** app-wide `.btn`/`.input` focus-visible rings (WCAG 2.4.7, guard test); approval-page split busy flags + approve confirm gate (BLOCKER); credit reject destructive styling. Plus detailed report `audit-reports/2026-06-15-overnight-ux-review.md` with a ~30-option what-to-order menu.
- `docs/portal-os/tranches/076-planning-copy-clarity.md` — planning copy clarity (2026-06-14 planning audit THEME P-F, final safe copy items): visible abbreviations expanded — `(eod)`→`(end of day)` on inventory-flow Projected-on-hand row (DayPopover + MobileDaySheet), `FG`→`finished-goods`/`Finished goods` in inventory-flow page/section descriptions + planning hub "FG evaluated" KPI label; "recs" left as-is (all code identifiers, never visible); status-term vocabulary (Superseded/Discarded) HELD — shared StatusBadge + admin, app-wide terminology decision
- `docs/portal-os/tranches/081-decision-board-premium.md` — Product Decision Board premium rebuild (Tranche 081, Playwright-iterated): money-framed verdict band (₪/yr at risk) with filter CTA, six clickable decision segments (Star/Gem/Workhorse/Drag/Loss/Dormant w/ contribution), upgraded zero-dep SVG quadrant (axis ticks + gridlines + tints + median/healthy reference lines + below-cost zone + staggered bubble animation + top-5 labels + hover crosshair), richer inspector w/ monthly sparkline + annualised contribution, sparkline trend table column, 30/90/180-day window toggle. New `@mocked` e2e `tests/e2e/decision-board.spec.ts` (15-product fixture, all categories). No backend change. tsc 0 / eslint 0 / playwright green
- `docs/portal-os/tranches/080-product-decision-board.md` — Product Decision Board (`/admin/decision-board`, planner+admin): finance decision surface ranking finished products for protect/promote/reprice/drop on margin × velocity. Joins existing `/api/economics` (COGS/margin/confidence/inventory) with a NEW `/api/orders/by-item-and-period` proxy (units sold/month from the LionWheel mirror) client-side — zero backend change, does not reopen the reverted 0210/0211 P&L-coverage work. KPI strip (profit pool 90d / losing-money / can't-decide / top-3 concentration), zero-dependency interactive SVG quadrant + inspector, ranked filterable sortable table, transparent tunable decision rules. Read-only v1; degrades gracefully when velocity is unavailable. tsc 0 / eslint 0
- `docs/portal-os/tranches/079-planning-a11y-interaction-completion.md` — planning a11y + interaction completion (2026-06-15 re-audit, accessibility+interaction lenses): new `useFocusTrap` hook; the 5 remaining production-plan dialogs (AddFromRecs/Edit/AddNote/EditNote/Cancel) gain focus mgmt + Escape + aria-labelledby + trap (075 did only ManualAdd); InventoryFlowTabs nav→div + tabpanels; MonthlyGrid aggregate rowheader→gridcell; forecast active-tab non-color weight; Toast role=alert for errors; required/aria-required + aria-describedby field errors + close aria-label; EditModal UoM input→select; per-card patch tracking + "another plan updating" tooltip (board writes stay serialized); RecommendationsToConvert retry (English, flagged) + reset-to-standard confirm; AddNote spinner; forecast remove-feedback + Back-unsaved guard; procurement loading skeleton; "not firmed"→"not yet confirmed". 677/677 green. Held: INTER-004/005 Hebrew confirms, FLOW-D backend, retry-button Hebraization
- `docs/portal-os/tranches/078-planning-data-cache-correctness.md` — planning data & cache correctness (2026-06-15 re-audit iteration, flow+interaction lenses): INTER-007 (P0 data integrity) forecast "Discard edits" left the 800ms autosave timer armed → discarded values POSTed; `useAutoSave` gains `cancel()` (clears buffer+timer), discard calls it (+test asserts it); FLOW-B `usePatchPlan`→invalidate `["planning"]`; FLOW-A/E inbox `RecommendationInlineCard` approve/dismiss now invalidates `["procurement","approved-purchase-recs"]`/`["rec-detail",id]`/`["planning"]`/`["inbox"]`; FLOW-C purchase-session→invalidate `["planning","overview"]`. Flagged (not changed): INTER-004/005 Hebrew procurement Skip/Place confirmations (need Hebrew copy approval), FLOW-D cross-run recs (backend endpoint). 677/677 green
- `docs/portal-os/tranches/077-planning-flow-correctness.md` — planning flow correctness (2026-06-14 planning audit THEME P-C F3 + sim dead-end): `useCreatePlan` now invalidates `["planning"]` + `["inbox"]` (not just `["production-plan"]`) so a recommendation consumed by plan-creation stops showing as actionable on runs/run-detail/recs/overview/inbox (double-consumption fix); SimulationResults fully-covered branch gains a "schedule it" CTA → production board (previously dead-ended on a green answer); footer "PACK recipe"/"BASE recipe"→"packaging recipe"/"liquid recipe" (COPY-004 residual). Verified F1/F2 already closed by 072; meeting link + inventory PO-rows confirmed not actionable. 677/677 green
- `docs/portal-os/tranches/075-planning-a11y-pack.md` — planning accessibility pack (2026-06-14 planning audit THEME P-G, P1 backlog after P0 clears): shared `useRovingTabList` hook → arrow-key + Home/End roving tabindex on forecast/runs/run-detail/InventoryFlowTabs tablists; MonthlyGrid orphan gridcells → proper grid/row/columnheader ancestry; ManualAddModal → dialog semantics (role+aria-modal+aria-labelledby+Escape+focus return); forecast publish outcome aria-live + role=alert; inventory-flow tab active state not color-only; scope=col on planning tables; skip-to-content link in planning shell; PublishGate text-[9px]→scale
- `docs/portal-os/tranches/074-boms-mock-data-honesty.md` — /planning/boms mock-data → honest empty states (2026-06-14 planning audit FLOW-007/VISUAL-012/VISUAL-014, last SHIP-blocking P0, decision delegated): fabricated KPI fallbacks (?? 92.4 / ?? 3 / ?? 4 …) → null + "—"/hidden; all mock-data panels (substitution, version timeline, price trend, stock coverage, packaging, approval, shortage forecast, multi-BOM cost, yield variance, changelog) → labeled EmptyState; header KPI chips trimmed to real-data-only; real sections (picker, simulator, net-requirements, at-risk, change-history/alternatives/validation/cost) kept; 677/677 green, build OK
- `docs/portal-os/tranches/073-planning-p0-clears.md` — planning P0 clears (2026-06-14 planning audit + /ux-release-gate HOLD): production-plan Move-to-tomorrow→useConfirm + HTTP-code banner removed; forecast filter count chips gated on data; inventory-flow/supply Hebrew labels→English (non-exception surface) + infrastructure jargon (Railway/stack trace/SQL pass/Endpoint/cold-start) scrubbed; production-simulation raw BOM-ID + supply-method enum + PACK/BASE→human; 677/677 green, build OK
- `docs/portal-os/tranches/072-runs-procurement-ordering-migration.md` — complete the runs→procurement ordering migration (2026-06-14 planning UX/UI audit, FLOW-002 + VISUAL-006, Tom decision C): recommendation→PO conversion moved into Procurement (new Hebrew RecommendationsToConvert section + _lib, with F1/F2 downstream+inbox invalidation); planning runs (`/planning/runs/[run_id]` + rec-detail) made diagnostic-only — approve/dismiss stay in the Inbox; broken `btn-accent`→`btn-primary`; 677/677 green, build OK
- `docs/portal-os/tranches/071-th-scope-a11y.md` — `<th scope="col">` a11y pass (2026-06-13 admin UX/UI audit A11Y-025 / WCAG 1.3.1): every data-table column header across 22 admin pages now carries scope="col" so screen readers associate header↔data cells; verified all `<th>` are in `<thead>` (none row headers); mechanical/additive, zero visual change; 673/673 green, build OK
- `docs/portal-os/tranches/070-copy-terminology-polish.md` — copy & terminology polish (2026-06-13 admin UX/UI audit THEME E tail, P2, pure UI copy): "live API"→"Live data" on 7 list headers; supplier-items aligned to "Sourcing links" (eyebrow + New button); groups SQL field names / regex out of operator copy (section descriptions, key rule, placeholder, permanent-key note); components read-only labels say what the operator can do + spelled-out abbreviations ("Code (locked)"→"Component ID (read-only)", "MOQ (purchase UOM)"→"Min. order quantity (purchase unit)", etc.); 673/673 green, build OK
- `docs/portal-os/tranches/069-operator-copy-clarity.md` — operator copy clarity + last modal a11y (2026-06-13 admin UX/UI audit THEME E + A11Y-008): holiday `ModalShell` rebuilt on Radix Dialog (focus trap / Escape / focus return / unique aria-labelledby); HTTP status/code prefix dropped from error banners across 7 admin pages (just err.message now); internal/dev references scrubbed from operator copy (holidays: soft-delete/W1 cycle 8/GAP-AHC-1/archived_at/live DB/upsert labels; jobs: W1 migrations/Edge Functions/jobs-registry caveat); friendly load-error templates (jobs/users/integrations); raw enum option text → human (supplier-items approval, sku-health + products/new supply method); 673/673 green, build OK
- `docs/portal-os/tranches/068-decision-grade-a11y-hardening.md` — decision-grade + P0 a11y hardening (2026-06-13 admin UX/UI audit): `ClassWEditDrawer` rebuilt on Radix Dialog (focus trap / Escape / aria-modal / focus return — A11Y-007); cost-draft **Approve** now confirms with old→new cost + delta (INTER-010); masters/archive **restore** mutations gain onError + success aria-live banner (INTER-015); holidays BulkImport **Commit** disabled when `validCount === 0`, preserving partial commit (INTER-012); 673/673 green, build OK
- `docs/portal-os/tranches/067-shared-primitives-pack.md` — shared-primitives pack (2026-06-13 admin UX/UI audit, cross-cutting THEMEs A/B/D/F): NEW accessible `useConfirm()`/`ConfirmDialog` (Radix, role=alertdialog, focus-managed, danger tone) replacing all 8 admin/economics `window.confirm()` sites; NEW `QueryCountChip` gating count chips on data-availability (items/suppliers/components/users — no more "0 items" during load); `aria-live`/role on items/suppliers/components feedback banners; `bg-bg-card`→`bg-bg-raised` (undeclared token → transparent surfaces) in DetailPage/ClassWEditDrawer/MasterSummaryCard/jobs; 673/673 green, build OK
- `docs/portal-os/tranches/066-admin-route-consolidation.md` — admin route consolidation (2026-06-13 admin UX/UI audit, THEME C): legacy `/admin/boms` tree (list + head + ~1588-line duplicate editor) → redirects to canonical `/admin/masters/boms`; the two parallel item-detail surfaces merged onto canonical `/admin/masters/items/[item_id]` (Product 360 + `/admin/items/[id]` → redirects, wizard lands canonical, BOM-display-only doctrine re-anchored); components "Used in" link repointed off the ignored `?head=` filter; 663/663 green, build OK
- `docs/portal-os/tranches/065-chain-audit-fixes.md` — chain-audit fixes (2026-06-12 forecast→receipt audit): cache-invalidation unification (GR post / manual PO / rec convert refresh PO surfaces), chain bridges (publish→meeting, firm→procurement, Procurement quick-action, simulation→procurement CTA + URL state), procurement inline supersede confirm + success banner + history link (Hebrew preserved), cadence-aware forecast staleness banner, PO pre-submit summary + human PO number + saved feedback + URL-synced filters, receipts two-step over-receipt confirm + forward links; 663/663 green
- `docs/portal-os/tranches/064-dashboard-state-legibility.md` — ux-flow-audit round-2 (FLOW-E01..E10): workload-meter state pill, dismissible queue hint, unified overdue vocabulary, refetchOnMount always, FG-node risk state, inline focus chip, urgency-graded procurement badges, prior-runs disambiguation, named since-chips, linked Awaiting-receipt row + TRUTH FIX: Materials node now reads supply-flow components (flow API is the FG projection)
- `docs/portal-os/tranches/063-dashboard-flow-audit-fixes.md` — ux-flow-audit round-1 fixes (FLOW-D01..D10: instruction banner + plain-language labels, focus eyebrow chip, 30s/window-focus refresh, mobile drill rows, per-source queue errors, 5-min delta threshold, worst-item Materials sub-line, Still-to-order label) + OUTBOUND node activated from the LionWheel mirror via new orders/outbound-summary proxy
- `docs/portal-os/tranches/062-loading-brand-logo.md` — GTLoader center monogram: 76px "GT" gradient text replaced with the real /brand/logo.png mark (92px, no theme invert needed on the always-dark loader); rings/glow/shimmer/timing untouched
- `docs/portal-os/tranches/061-dashboard-condense.md` — dashboard Phase 3: 10→7 sections (duplication map), NEW Week panel (₪ to order this week / approved-not-recorded-as-PO / awaiting receipt + Receive CTA, week run progress, pure `_lib/week.ts`), beauty pass (premium flow nodes, longer connectors, dot-grid faded, hero breathe retired, chart peak gridline), exceptions query removed; 622/622 green
- `docs/portal-os/tranches/060-dashboard-hierarchy.md` — dashboard Phase 2: VerdictBand + deterministic Focus Engine (7-rule cascade) + since-last-look chips + sticky verdict mirror, Factory Flow Ribbon (5 nodes, edges animate only on real same-day movement), Today's Work unified queue (severity→category→age, cap 8), role content rules, new @mocked dashboard e2e; 616/616 green
- `docs/portal-os/tranches/059-dashboard-truth.md` — dashboard Phase 1 (P0 trust): useNow live clock, CountUp prev→new tween + StrictMode fix, exceptions headline=critical, ILS-only PO sums, local-time day boundaries, /inbox honors ?id=; 596/596 green
- `docs/portal-os/tranches/058-inventory-flow-production-lens.md` — Production lens: mobile day cells show EOD/−gap numbers in desktop 5-tier palette, card digest row (On hand / Demand 14d / Unfilled-or-Incoming 14d), "Covered by plan" badge + truthful insight, NEW pure `_lib/production-lens.ts` with URL-backed ?sort= ordering (Urgency/Biggest gap/Demand/Product line) honored by mobile stream AND desktop grid; 587/587 green (+11)
- `docs/portal-os/tranches/057-inventory-flow-mobile-pack.md` — Inventory-flow mobile pack (ux-flow-audit 18 findings): NEW MobileDaySheet day-detail bottom sheet + 7×2 tappable day-strip grid (44px targets, P0 FLOW-M04/M05/M13), useMediaQuery boolean|null skeleton gate, filter-bar-sticky below TopBar, section header, CTA/chips/KPI/tab mobile fixes, "Product line" label; 576/576 green
- `docs/portal-os/tranches/056-mobile-nav-redesign.md` — Mobile nav redesign (Tom 2026-06-12): bottom tab bar REMOVED (MobileBottomNav + bottom-nav.ts deleted, FLOW-016 remediation intentionally reversed), drawer becomes first-class (SideNav density prop, 44px rows, active-item scrollIntoView), TopBar shows current page label <sm via new pure `src/lib/nav/active.ts`; 576/576 green (−12/+8 tests)
- `docs/portal-os/tranches/055-final-deletion-superseded-planning.md` — PROPOSED, Sunday-gated: delete the three superseded planning redirect stubs after one clean procurement Sunday
- `docs/portal-os/tranches/051-mobile-shell-pack.md` — Mobile Tranche B: MobileBottomNav 5-tab bar (<md, manifest-derived roles), PO list mobile cards, reusable ScrollFade scroll affordance (quick actions / group chips / inbox chips), inbox 32px touch targets, hero chip fmtILSCompact; 515 passed +12 new
- `docs/portal-os/tranches/052-recipe-override-ui.md` — "Improvised liquid recipe" UI (backend 0237): RecipeOverridePanel per-plan liquid editor (diff-vs-standard chips, availability tiers, remove/restore/add-RM, load-last-improvisation, reset-to-standard, identical-set saves as clear), ManualAdd "Review recipe" step + quiet plain-add path, ProductionJobCard "Custom recipe" chip (lazy flag cache) + Adjust-recipe action, production-actual from_plan_id open + custom-recipe banner, 2 new recipe proxies, pure tested `_lib/recipe-helpers.ts`; +26 tests
- `docs/portal-os/tranches/053-mobile-planning-pack.md` — Mobile Tranche C (planning surfaces): forecast MonthlyGrid <768px collapsible per-item list (44px inputs, same auto-save pipeline), window.confirm → removal bottom-sheet + inline two-step discard, `.fc-bottom-bar` safe-area inset, procurement calendar <md grouped-by-week list (tierChip/tierDot reuse), PlanningSubNav active-tab scrollIntoView + ScrollFade, meeting week-selector min-w-0 + Generate own row + CadenceRail one-row 390px (corner Today dot); 567/567 green (+25 new)
- `docs/portal-os/tranches/054-mobile-closing-pack.md` — Final mobile closing pack: production-plan board auto-centers the TODAY lane on load (once per week view, only when overflowing) + "Today" jump button (<md always / md+ when out of view), lane floor 196→140px <md, week nav collapses to icon-only Prev/Next/Refresh with range line above + Updated caption below, footer "View inventory impact" link visible at all widths, AddFromRecs sheet max-h[min(90vh,600px)], admin/economics gap-table overflow-x-auto (only admin table missing a wrapper; 16 other listed pages already safe); pure tested `_lib/board-scroll.ts`; 580/580 green (+13 new)
- `docs/portal-os/tranches/049-design-system-sweep.md` — Phase 7 sweep: Lucide-only, btn unification, table-base, WorkflowHeader size prop, SectionHeading, shared feedback states, stat-card consolidation
- `docs/portal-os/tranches/048-production-board-pack.md` — Phase 6: one-tap as-planned, re-plan remainder, UoM select, board refresh, Today strip, danger cancel
- `docs/portal-os/tranches/047-procurement-po-interaction-pack.md` — Phase 6: supplier comparison strip, lead-time dates, truncation banner, Clear-all, publish isSubmitting
- `docs/portal-os/tranches/046-os-truthfulness-role-gates.md` — Phase 5: manifest regeneration 41->77, baseline anchors, middleware reconciliation, honesty labels
- `docs/portal-os/tranches/045-planning-consolidation.md` — Phase 4: runs demoted, superseded pages -> redirects, cadence block
- `docs/portal-os/tranches/044-groups-v1-portal.md` — Phase 3: shared taxonomy, group filters, /admin/groups
- `docs/portal-os/tranches/043-po-price-entry-cost-drafts.md` — Phase 2: PO price entry, catalog write-back confirm, cost-drafts queue
- `docs/portal-os/tranches/042-refresh-invalidation-design-p0.md` — Phase 2/7 hotfixes: /inventory 60s live refresh, cache-invalidation sweep (economics/approvals/receipts/PO place), dashboard source labels + value-trend honesty, kpi-tile dedupe, italic removal
- `docs/portal-os/tranches/041-journey-404-deadend-ledger-safety.md` — Phase 1 hotfixes: journey 404s (`/ops` prefix, `/stock/ledger`), receipts locked-PO dead-end, `/admin` index + 4 broken admin links, loss-waste confirm gate, scrap-consumption copy truth (T1), dropped deep-link params, focus-mode PO link, inventory drill-down repair
- `docs/portal-os/tranches/040-procurement-rtl.md` — `/planning/procurement` (a fully-Hebrew surface) gets `dir="rtl"` on its page root so the body reads right-to-left while the app shell stays LTR; single-attribute presentation change, copy stays Hebrew, all testids preserved, 416/416 green
- `docs/portal-os/design-readiness/README.md` — design-system restyle-readiness foundation: index + the token→css-var→surface leverage chain + where the truth lives + readiness-at-a-glance (Tranche 072 design-prep, for a future system-wide premium visual upgrade).
- `docs/portal-os/design-readiness/tokens.md` — canonical reference of every design token (the restyle knobs): color/type/radius/shadow/spacing/motion with light+dark values and the restyle leverage order.
- `docs/portal-os/design-readiness/primitives.md` — component vocabulary: the `.btn/.card/.chip/.input/.stat-card` CSS-class primitives + where the React-primitive layer is thin (no Button/Input/Card) and the adoption hotspots.
- `docs/portal-os/design-readiness/design-debt.md` — quantified token-bypassing debt (raw font-sizes, `text-[Npx]`, off-system shadows, off-system credit-tracking page, ghost tokens) with file:line and `[NOW]/[DECIDE]/[BUG]` tags; readiness 72/100.
- `docs/portal-os/design-readiness/restyle-playbook.md` — the ordered restyle procedure + PREP-01..10 preparation backlog + owner-decision list + drift-prevention convention rules.
- `docs/portal-os/tranches/073-ux-gate-p1-backlog.md` — PROPOSED tranche: the verified P1 backlog from the 2026-06-17 `/ux-release-gate`, grouped into 4 bounded batches (stock-write safety, copy hygiene, visual tokens, a11y); P0-1 already fixed, Hebrew authorized in CLAUDE.md.
- `docs/ux/ux-release-gate-2026-06-17.md` — gate record for PR #95: 5-dimension audit, governor verification (16 raw P0s → 1 verified), Tom decisions, CONDITIONAL_SHIP verdict.
- `docs/portal-os/audit-reports/2026-06-19-ux-mobile-audit.md` — read-only UX/mobile audit of all ~80 pages; finding: portal already premium (88/100); ranked list-page mobile-card program; flags what NOT to churn + backend-blocked gaps.
- `docs/portal-os/tranches/082-admin-suppliers-mobile-cards.md` — adds `<md` mobile card view to the suppliers master list (table preserved at md+); pattern-setter for the list-page mobile-card program.
- `docs/portal-os/tranches/083-admin-items-mobile-cards.md` — adds `<md` mobile card view to the items master list (name/health/status + facts + inline group assign + actions; table preserved at md+).
- `docs/portal-os/tranches/084-admin-components-mobile-cards.md` — adds `<md` mobile card view to the components (RM/PKG) master list (category/readiness/status + unit/lead/supplier + actions; table preserved at md+).
- `docs/portal-os/mobile-perfection-program.md` — Tom-directed program: make every page perfect on mobile via ui-ux-pro-max + Playwright visual iteration; holds the proven screenshot recipe, priority order, mobile rubric, and progress tracker.
- `docs/portal-os/tranches/085-planning-mobile-touch-targets.md` — PlanningSubNav tabs get `min-h-[44px]` (WCAG/HIG touch target) across every /planning/* page; verified with an iPhone-14 Playwright shot.
- `docs/portal-os/tranches/086-simplify-items-card-casepack.md` — `/simplify` cleanup: single null-guard for the items card case-pack cell (was a double-tested triple ternary); behavior-identical.
- `docs/portal-os/tranches/086-procurement-placement-queue.md` — EXECUTED (Tom end-to-end, draft PR): Tom→office-manager procurement handoff. Backend (gt-factory-os #91, merged) = APPROVED_TO_ORDER state + `payment_terms` + place-order API; portal = bookkeeper placement queue (`/purchase-orders/placement-queue`, planning:execute, Hebrew/RTL) + one-tap full goods receipt + PO-list "To place" surfacing. /ux-flow-audit + /ui-ux-pro-max verified. (Number 086 shared with the simplify-items entry above — distinct slugs; code+backend already reference "tranche 086".)
- `docs/portal-os/tranches/087-production-plan-ux-fixes.md` — render-grade `/ux-release-gate` findings on `/planning/production-plan`: landed F8 (`Rec`→`Recommended` chip), F9 (de-duplicated subtitle/banner copy), F5 (shared `WorkflowHeader` header actions wrap on mobile — primary CTA no longer clipped); corrected F7/F10 (not bugs); backend requirements for F3 capacity + F4 rec rationale handed to W1/W4 (`docs/ux/production-plan-backend-requirements-2026-06-25.md`); F1/F2/F6 staged.
- `docs/portal-os/tranches/088-null-safety-error-leak-hotfix.md` — P0 cluster from the render-grade 3-area sweep (`/stock/*`, `/inbox`, `/planning/inventory-flow`): `error.tsx` stops leaking raw JS exceptions to operators (non-prod only); inbox `5/4`→`/TOTAL_SOURCES` + false "all-clear" gated on `allSourcesFailed`; null-guards on `InventoryFlowClient` `data.items`, `receipts` `.rows.find`, and `me/activity` `flatMap` so degraded payloads render instead of crashing.
- `docs/portal-os/tranches/089-inbox-cache-coherence.md` — flow-continuity audit P2 fix (2026-06-25): the two stock approval-hold paths (`/stock/waste-adjustments`, `/stock/physical-count`) now `invalidateQueries({ queryKey: ["inbox"] })` when a submit is held for planner approval, so the new approval shows in `/inbox` immediately instead of after the 30s staleTime; mirrors the existing `RecommendationsToConvert.tsx:56` idiom; tsc 0 / vitest 775/775.
- `docs/portal-os/tranches/113-kpi-honesty-and-confirm-polish.md` — KPI unit-honesty + confirm a11y polish (2026-06-26, Tom-approved decisions + /ck:review survivors): FLOW-014/015 the "N units total" KPI summed planned_qty across mixed units (liters+bottles+kg) into a meaningless number → now shows the qty+uom only when units are uniform, else the honest run count ("N planned runs"); dominantUom day-lane fallback untouched. H1 RecipeOverridePanel discard confirm moves focus to its "Keep editing" default on open (keepEditingRef); H2 Escape dismisses the confirm (back to form), not the whole panel. Decisions recorded (not building in v1): Batch 7 research features (need capacity/lead-time model CLAUDE.md locks out of v1, or are layout opinions) + confirm-panel family (no new modal — confirm only on irreversible actions via the existing inline two-step; reversible auditable actions stay confirm-free for operator speed). tsc 0, eslint 0, vitest 795/795.
- `docs/portal-os/tranches/112-deepen-dialog-a11y-hook.md` — /deepen dialog-shell consolidation (2026-06-26, Batch 6): the seven inline modals in production-plan's 2535-line page.tsx each hand-copied the identical ~26-line dialog focus contract (3 refs + useFocusTrap + focus capture/restore useEffect + Escape/trap onKeyDown). Extracted to one hook `_lib/useDialogA11y.ts` — callers spread `{dialogRef, titleRef, onKeyDown}` onto existing markup, DOM/behaviour unchanged. page.tsx 2535→2399 (~136 lines removed); useFocusTrap imported once not 7×. §V test `_lib/useDialogA11y.test.tsx` (5 cases) locks the contract (Escape closes / ignored-while-busy / non-Escape no-op / initial focus / focus restore). Behavior held: suite green before (790) AND after (795 = 790 + 5 new). Follow-up: RecipeOverridePanel adopt the hook. tsc 0, eslint 0, vitest 795/795.
- `docs/portal-os/tranches/111-production-plan-visual-tokens.md` — production-plan visual token hygiene (2026-06-26, /ux-release-gate Batch 4): VISUAL-001 `shadow-2xl`→`shadow-pop` on all 9 modal/overlay containers (off-system stock shadow → design-system token); VISUAL-002 `text-[9px]`→`text-3xs` on 13 micro-labels (below-scale arbitrary → smallest scale token, 9→10px; explicit tracking/leading classes already win so only the size snaps); `tracking-wider`→`tracking-sops` on 4 uppercase eyebrow labels. Pure presentation, no behavior change. Follow-up: text-[26px] hero + rail inline heights (no matching scale token). tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/110-production-plan-accessibility.md` — production-plan accessibility (2026-06-26, /ux-release-gate Batch 5): A11Y-002 day-lane footer "Production"/"Note" buttons get `min-h-[32px]` + day-scoped aria-labels; A11Y-004 WeekTimelineRail notch dots get `role="img"`+aria-label ("All completed"/"Overdue") instead of color-only (neutral dot aria-hidden); A11Y-006 the horizontal weekly board is now `tabIndex=0`+`role=region`+label+focus-ring (axe scrollable-region-focusable); A11Y-008 SHARED `SearchableSelect` options `<button role="option">`→`<div role="option">` (invalid role for button) + `aria-activedescendant`/`role=combobox`/`aria-controls` on the search input — keyboard model unchanged (input+activeIndex), behaviour + testids preserved; card status icons (Clock/CheckCircle2/Ban) aria-hidden (state already in text). tsc 0, eslint 0, vitest 790/790 (shared primitive broke no consumer).
- `docs/portal-os/tranches/109-production-plan-interaction-completeness.md` — production-plan interaction completeness (2026-06-26, /ux-release-gate Batch 3): FLOW-023 `useSavePlanRecipe` now invalidates the per-plan `["bom-impact", itemId, planId]` snapshot (predicate-scoped) so the card's material breakdown isn't stale after a recipe save; FLOW-024 `useDeletePlan` now invalidates `["inbox"]` (a delete frees the linked rec back to the pool — mirrors `useCreatePlan`'s consume-side invalidation); INTER-003 EditModal blocks a blank/zero/NaN planned-qty submit with an inline `role="alert"` error (`aria-invalid`/`aria-describedby`) instead of posting NaN, and fixes the subtitle `item_name ?? item_id` §1 leak → "Unnamed item". INTER-001 (double-submit on add) verified ALREADY handled across all 4 modals (`!isSubmitting`/`canSubmit`) — no churn. tsc 0, eslint 0, vitest 790/790. Follow-up: patch 422 per-field mapping.
- `docs/portal-os/tranches/108-production-plan-copy-vocabulary.md` — production-plan copy/vocabulary (2026-06-26, /ux-release-gate Batch 2): §1 jargon out of VARIANCE_TOOLTIP ("production reporting v1 model…" → plain English) + "Firm this plan"→"Confirm this plan"; §4 "✓ Done"→"Completed" day-lane badge (drops decorative Unicode ✓); §8 card CTA "Report"→"Report Production"; edit/cancel/delete/move-to-tomorrow success toasts now name the affected plan via a new §1-safe `planLabel()` (item name or base-batch descriptor, never raw item_id); move-confirm `item_name ?? item_id` §1 leak fixed; internal `mapStatusToHebrew`→`mapStatusToMessage` (misleading name, produces English). Verified non-issues: header CTA already "Add production"; production-actual tooltip out of scope. Deferred to Tom: mixed-UoM "units total" KPI sums incommensurable units (FLOW-014/015 — product call). tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/107-production-plan-p0-safety.md` — production-plan P0 cluster (2026-06-26, /ux-release-gate Batch 1 of the approved 7-batch plan): (1) RecipeOverridePanel discarded unsaved per-run recipe edits silently on ×/Cancel/backdrop/Escape and had no focus-trap/Escape/initial-focus/`aria-labelledby` — now adopts the page's modal a11y pattern + a discard confirm via `requestClose` (INTER-006/FLOW-012/A11Y-005/A11Y-009); (2) §1 leak — `useCreatePlan`/`usePatchPlan` appended raw 422 `detail` (Zod paths/enums) to operator toasts, now plain mapped status only, structured `validation_errors` still field-mapped (FLOW-018/019); (3) ProductionJobCard title fell through to raw `item_id` → "Unnamed item" (COPY-002); (4) done-plan "View report →" link was gated on `varianceSign` and vanished when variance couldn't be computed → decoupled, link shows whenever `submission_id` exists (FLOW-007). tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/106-procurement-invalid-qty-block.md` — procurement correctness (2026-06-26, /portal-audit interaction-design-specialist INTER-005): the focus line editor's `saveEdit` silently reverted an invalid qty to the saved value and posted "successfully" (planner thought they changed it; old qty sent). Now validates non-dropped lines and BLOCKS the save with an inline error instead of silently reverting. tsc 0, eslint 0, vitest 790/790. All correctness/data-loss findings on the procurement surface now closed.
- `docs/portal-os/tranches/105-procurement-unsaved-edit-guard.md` — procurement data-loss fix (2026-06-26, /portal-audit interaction-design-specialist INTER-004, P0): the focus overlay Escape/× silently discarded unsaved line-quantity edits. FocusCard now reports `onDirtyChange`; FocusMode's `requestClose` shows an inline confirm ("שינויים שלא נשמרו · סגור בכל זאת / המשך עריכה") when dirty, else closes normally. Non-dirty path unchanged (all 6 FocusMode tests pass). tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/104-procurement-jargon-label.md` — procurement copy (2026-06-26, /portal-audit interaction-design-specialist INTER-010): the focus-card add-line form rendered raw internal `selected.kind` ("item"/"component") as meta text (§1 violation) → mapped to Hebrew labels (רכיב/פריט) on this authorized Hebrew surface. **INTER-011 rejected** (verified false: next-at-last-position intentionally completes the walk-through — broke 2 FocusMode tests; reverted). tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/103-component-archive-banner.md` — admin feedback (2026-06-26, /portal-audit interaction-design-specialist INTER-004, partial): `/admin/masters/components/[component_id]` archive/restore now shows a success banner (wired `statusMutation.onSuccess` to the page's existing `editBanner`) — was a silent drawer close. Item + supplier archive banners + INTER-011 deferred (need new banner plumbing / useSearchParams). tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/102-admin-per-row-status-disable.md` — admin responsiveness (2026-06-26, /portal-audit interaction-design-specialist INTER-001): the items/components/suppliers list Deactivate/Activate buttons disabled the WHOLE table during one row's mutation (shared `statusMutation.isPending`). Now gated per-row via `statusMutation.variables?.<id> === r.<id>` (table + mobile card, all 3 pages) — only the active row disables. tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/101-admin-save-feedback.md` — admin save feedback (2026-06-26, /portal-audit interaction-design-specialist): inline field-save **success confirmation** added on `/admin/masters/items/[item_id]` (INTER-003) + `/admin/masters/suppliers/[supplier_id]` (INTER-008) — was silent on success; supplier `CostEditCell` save error moved from a `title=` tooltip to **visible text** (INTER-009). tsc 0, eslint 0, vitest 790/790. Remaining admin-feedback items (INTER-001/002/004/011/005) follow-up.
- `docs/portal-os/tranches/100-admin-primary-supplier-safety.md` — admin safety (2026-06-26, /portal-audit interaction-design-specialist on admin master-data): INTER-006 (P0 — `/admin/components` changing a component's primary supplier now confirms via the existing accessible `useConfirm`, matching the detail page; was a silent replace of planning-critical cost/lead-time/MOQ) + INTER-007 (component detail "Promote" button disabled + "Promoting…" while the mutation is in flight — no duplicate PATCH on double-click). tsc 0, eslint 0, vitest 790/790. Admin feedback cluster (INTER-001/002/003/004/008/009/011) triaged as follow-up.
- `docs/portal-os/tranches/099-inbox-inline-card-skeleton.md` — loading polish (2026-06-26, /portal-audit interaction-design-specialist INTER-007): `ApprovalInlineCard` detail-loading state upgraded from a single pulsing text line to a 2-row `aria-busy` skeleton grid (stable size anchor, no layout jump). Closes the inbox audit's safe findings; INTER-001/002 (confirm panels) held for Tom, INTER-008 (comment honesty) follow-up. tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/098-inbox-bulk-confirm-inline.md` — robustness (2026-06-26, /portal-audit interaction-design-specialist INTER-003): `/inbox` bulk-resolve (destructive, irreversible) no longer gates on `window.confirm` (silently fails in some WebViews + untestable) — replaced with a two-step inline confirm in `BulkActionBar` (arm → "Confirm — resolve N" / Cancel), `⌘⏎` arms-then-confirms. No added friction (replaces an existing confirm). tsc 0, eslint 0, vitest 790/790.
- `docs/portal-os/tranches/097-inbox-approvals-safety.md` — approval-surface safety (2026-06-26, /portal-audit interaction-design-specialist): INTER-005 (inline approve/reject now invalidates the inbox queue via a threaded `onActionComplete` → no stale lingering row, cf. tranche 089), INTER-006 (waste + physical-count detail Approve/Reject disabled while detail is loading — no approving unseen submissions), INTER-004 (keyboard `a` acknowledge guarded against double-fire). Correctness/safety, no flow change. tsc 0, eslint 0, vitest 790/790. Confirm-adding findings (INTER-001/002) held for Tom.
- `docs/portal-os/tranches/096-fetchjson-admin-migration.md` — dedup continuation (2026-06-26, tranche 093 follow-up): migrated the 11 admin pages whose `fetchJson` was BYTE-IDENTICAL to the shared `src/lib/http/fetchJson.ts` (verified by exact-string match) — local copies deleted, shared helper imported. Behavior held by construction; divergent copies left untouched. tsc 0, eslint 0, vitest 790/790. ~15 identical/divergent copies remain as follow-up.
- `docs/portal-os/tranches/095-stock-form-interaction-polish.md` — interaction polish (2026-06-25, /portal-audit interaction-design-specialist): INTER-004 (express receive button spinner), INTER-005 (decrease stepper disabled at qty≤1), INTER-008 (Yes-cancel in-flight spinner), INTER-011 (PO-lines error Retry button), INTER-012 (master-load Retry underline→`btn btn-sm` on waste + physical-count). Feedback/consistency only, no flow change. tsc 0, eslint 0, vitest 790/790. Confirm-panel family (INTER-001/003/006/007) held for Tom.
- `docs/portal-os/tranches/094-idempotency-key-lifecycle.md` — stock-truth fix (2026-06-25, /portal-audit interaction-design-specialist INTER-002): the 3 operator stock forms (`receipts`, `waste-adjustments`, `physical-count`) generated the submit `idempotency_key` inline per submit, so a retry after a lost response posted a DUPLICATE ledger event. Now each holds the key in a `useRef`, reused across retries, cleared only on success/reset → backend dedups the replay. §V guard test. vitest 787→790, tsc 0, eslint 0. (Remaining INTER-* findings triaged in the tranche; confirm-panel additions held for Tom.)
- `docs/portal-os/tranches/093-deepen-fetchjson-stock-forms.md` — `/deepen` continuation (2026-06-25): extract the per-page `fetchJson<T>` (authed JSON GET + operator load-error copy, hand-defined 33× across the app) into one canonical `src/lib/http/fetchJson.ts`; migrate the 3 byte-identical stock-form callers (`waste-adjustments`, `physical-count`, `receipts`) — surgical, behavior held (bodies were identical), `production-actual` (divergent copy) + ~29 other sites tracked as follow-up. vitest 784→787, tsc 0, eslint 0.
- `docs/portal-os/tranches/092-scorecard-truth-correction.md` — scorecard truth correction (2026-06-25, /portal-audit admin-surface auditor): admin_superuser_depth 5→7 (the three admin shells `/admin/users` + `/admin/jobs` + `/admin/integrations` are live real-data surfaces — `QuarantinedPage` no longer exists in `src/app`; backend-package #3 shipped, #4/#5 read-halves shipped) + nav_integrity 9→10 (Tranche 090 baseline-update ritual + manifest-orphan closure). Total 88→91 (truth correction, not new work). scorecard.json/.md + backend-package status banner; JSON valid, category sum == total.
- `docs/portal-os/tranches/091-deepen-stock-submit.md` — `/deepen` design pass (2026-06-25, Tom-approved): new deep `src/lib/stock/submit.ts` `submitStockEvent` hides the POST + JSON-safe parse + `{posted|pending|rejected|network}` discrimination + the portal_ux_standard §1 "never leak raw JSON to operator" extraction that `/stock/waste-adjustments` + `/stock/physical-count` each hand-rolled; both refactored to a `switch (result.kind)` (behavior held exactly); §V guard test pins the submit path so a future form can't re-inline it. Pure structure — vitest 775→784 (775 held + 9 new), tsc 0, eslint 0. Follow-up: production-actual deepen pass.
- `docs/portal-os/tranches/090-os-drift-resync.md` — OS drift re-sync (2026-06-25 /portal-audit, route-auditor + regression-sentinel, both PASS): `route-manifest.json` +3 orphan rows (`/admin/decision-board`, `/credit-tracking`, `/inbox/approvals/inventory-movement/[submission_id]` — live code+nav with no row); `quarantine.json` +1 `pending-cleanup` for `tests/e2e/ux-shot.spec.ts` forbidden-string doc comment; `baseline.json` `kind=baseline-update` re-anchor (sha→096467f) capturing 3 authorized nav additions + the `/inventory/bulk-count` role-gate. Docs/state only, additive; no source, no backend. Stops false-positive drift on future audits.
- `docs/portal-os/tranches/089-deepen-useflowitems.md` — `/deepen` pass: extracted the byte-identical `data → {filteredItems, families}` derivation (duplicated in `InventoryFlowClient` + `SupplyFlowClient`, the tranche-088 double-fix site) into one deep hook `_lib/useFlowItems.ts`. Pure structural; behavior held (vitest 59=59, render unchanged); −40 lines; the filter+null-safety decision now lives in one place and can't re-diverge.
- `docs/portal-os/tranches/090-nav-corridor-redesign.md` — Tom-directed nav corridor redesign (deep-research + `/ck:grill`, 2026-06-26): card-home landing + thin role-tailored sidebar; navigation/visual-design only, no backend, no route deletion, admin keeps full access (collapsed ≠ removed). Slice A (landed): `Stock`/`Planning`/`Purchase Orders` nav groups collapsible+default-collapsed (progressive disclosure) so the sidebar stops being a flat ~30-item haystack; manifest tests green, 789/789 vitest. Slice B (landed, #143): `/home` card-landing + landing route. Slice B / Phase 2 (Tom-directed 2026-06-26): role-tailored striking card-home — "The Line" cockpit bento (one primary tile per role + grouped tiles + petrol-teal spine-that-fills-on-hover), Hebrew-RTL bookkeeper (viewer) view; tile model `src/features/home/cockpit.ts`, tile `src/app/(shared)/home/_components/HomeTile.tsx`; +21 cockpit tests (810/810).
- `docs/portal-os/tranches/114-decision-board-ui-amplify.md` — Tom-directed (2026-06-26) decision-board UI amplify: elevate `/admin/decision-board` from premium-v1 (081) to a signature, decision-grade instrument at the top of the "Operational Precision" identity. Visual/presentation only — no backend, no shared-component/globals/tailwind change, data contract unchanged. Signature = the margin×velocity portfolio map (depth, calm star-glow, gradient bubbles, readout-coupled inspector); decision headline as status readout w/ animated profit-pool meter; six buckets → cohesive portfolio strip w/ share micro-bars; animated count-ups (reduced-motion safe), skeleton loaders, refined type/dividers, dark-mode correct. e2e `/api/economics` fixture refreshed to the current 90d read model (qty_sold_90d/order_count_90d/units_prev_90d); testids preserved. Playwright-iterated.
- `docs/portal-os/tranches/116-production-plan-stock-context.md` — landed & merged (2026-07-02, Tom-directed, PR #151, squash `e9744fd`): stock-timing context strip on `/planning/production-plan` — five decision variables (on-hand, produce-by deadline, daily demand, cover-after-run, covered-by-plan) in ManualAddModal + InventoryImpactPanel, fed by the existing `/api/inventory/flow` shared cache; no backend, no RM scope. Passed a 6-audit `/ux-release-gate` sweep (0 P0s) and end-to-end `/verify`.
- `docs/portal-os/tranches/117-production-plan-visual-amplify.md` — proposed (2026-07-02, Tom-directed via `/frontend-design`): visual amplify pass on `/planning/production-plan` — compresses 7 stacked chrome layers into one status bar, elevates `WeekTimelineRail` into a real production-rhythm signature (baseline tracks, today-anchor band), and establishes `font-mono` (IBM Plex Mono, already portal-wide infra) as the page's numeric language. No new tokens/fonts, no backend/logic change — presentation-only across 6 files.
- `docs/portal-os/tranches/115-decision-board-a11y-touch.md` — follow-on to 114 from running the shipped board through `/frontend-design` + `/ui-ux-pro-max` + `/ux-flow-audit`. Fixes the `ux-flow-architect` findings (page-local, no backend): FLOW-001 (P0) the Inspector was hover-only → quadrant bubbles + table rows now select on tap/click/focus/Enter (sticky selection) and the SVG is opened to AT (group role + per-bubble button role/aria-label); FLOW-002 removed pointer-only "hover" copy; FLOW-003 full `/api/economics` failure now shows an honest error state + Retry instead of a zero-data "all priced above cost" verdict; FLOW-005 RulesPopover dismisses on Escape/outside-click; FLOW-007 restored the "Needs data" segment card to the filter strip. FLOW-006 (action→destination links) deferred. e2e adds tap + keyboard inspection + needs_data assertions. tsc 0 / eslint 0 / vitest 809 / playwright green.
- `docs/portal-os/tranches/118-movement-log-fg-out-pause.md` — Tom-directed (2026-07-02, extended 2026-07-03; renumbered 116→118 — 116 was claimed concurrently by tranche 116-production-plan-stock-context, PR #151, which merged to main first): `/stock/movement-log` gets (1) a "pause LionWheel FG-out inventory decrements" toggle so the weekly physical count isn't double-counted by deliveries LionWheel confirms after the count runs — `FgOutPauseControl`, admin/planner-only toggle, warning banner visible to every role while paused, confirm-before-acting, honest load-error state (never implies "not paused" if the state can't be read); and (2) a per-delivery "Undo this delivery" control in the details drawer for a FG_OUT_PICK that already posted before the pause was switched on — `FgOutPickUndoControl`, admin/planner-only, no time limit, optional reason, hides once already reversed, surfaces (not silently skips) the rare dual-role bulk-cover gap. Scope for both grilled with Tom via `/ck:grill`. Backend companion (migration 0277 + reconciler skip-and-mark branch + fg-out-pause endpoint + fg-out-pick-reversal endpoint, reusing the already-ratified `FG_OUT_PICK_REVERSAL` movement type) lands in `gt-factory-os`, same branch (merged: PR #143). English/LTR — not a Hebrew-exception surface. tsc 0, vitest 15/15 new across both controls.
- `docs/portal-os/tranches/119-home-ux-gate-p1.md` — Tom-approved (2026-07-03; renumbered 091→119, 091 was already claimed by tranche 091-deepen-stock-submit): executes all 10 verified P1 conditions from the post-merge `/ux-release-gate` CONDITIONAL_SHIP verdict on `/home` (tranche 090 Slice B + Phase 2, PR #145). Hebrew `lang="he"` on the viewer cockpit div (A11Y-002); `/admin/decision-board` middleware carve-out for planner (FLOW-002, +2 regression tests); `.reveal` entrance animation replaced with Tailwind's existing `animate-fade-in-up` + `motion-reduce:animate-none` so reduced-motion is honored without touching `globals.css` (INTER-001/A11Y-003); `⌘K`→`Ctrl+K` wrapped in `<bdi dir="ltr">` fixing the RTL bidi-reversal in the Hebrew subcopy (COPY-003/FLOW-003); responsive `md:block`/`md:hidden` copy so mobile stops promising a search shortcut the hidden-below-md CommandPalette trigger can't deliver (FLOW-001); de-jargoned two tile blurbs ("BOM-derived"→active-recipe language, "anchor on approval"→plain stock-truth language — COPY-001/002); "Production report"→"Production Report" tile + synced stale sidebar label "Production Actual"→"Production Report" (COPY-004); `text-fg-subtle`→`text-fg-muted` component-level contrast fix, 3.09:1→5.52:1 (A11Y-001, token-level question deferred to Tom); single-tile-group `sm:col-span-2 lg:col-span-3` span-fill rule (VISUAL-007). VISUAL-009 dismissed as a Next.js dev-mode-indicator artifact, not real. 17 P2s deferred to a future pass. Gate report: `PRODUCTION/docs/phase8/dry-runs/2026-07-03-ux-release-gate-home.md`. tsc 0, eslint 0, vitest green (+4 new tests).
- `docs/portal-os/tranches/120-home-signature-glow.md` — Tom-directed (2026-07-03, via `/ui-ux-pro-max`, chose "Signature Glow" restrained over "Full Radiance"): visual amplify pass, primary hero tile ONLY on `/home` — mirrors the dashboard's own already-shipped `.dash-hero` glass+glow recipe (globals.css) rather than inventing a new visual language: translucent `backdrop-blur` surface, static top-anchored two-stop accent radial glow (RTL-mirrored), hairline accent line at the top edge, layered ambient shadow at rest. Deliberately static (no pulse) per the dash-hero precedent's own retired-animation "motion budget" doctrine. Every other tile ("The Line" spine/fill/hover) unchanged. Zero new tokens — every value is an existing Tailwind utility or inline `style` referencing an existing CSS custom property (`--accent`/`--info`/`--shadow-color`/`--shadow-color-deep`); `tailwind.config.ts`/`globals.css` untouched. tsc 0, eslint 0, vitest 871/871 unchanged (visual-only).
- `docs/portal-os/tranches/121-thursday-corridor-p0.md` — Tom-approved (2026-07-03, DR-018 `/ux-release-gate` P0 batch; renumbered 116→121 — 116-120 were already claimed by unrelated, already-merged tranches): closes all 5 P0 findings blocking the Thursday→Sunday production-planning corridor. FLOW-001: "Weekly Meeting" added to the Planning nav group (was reachable only by deep link). VISUAL-001: `.btn-accent` aliased to `.btn-primary` in `globals.css` (Tom-locked decision) — fixes 10+ primary CTAs across procurement/PO that were rendering unstyled. INTER-001: "Generate / refresh drafts" (which silently overwrites hand-edited drafts) now requires an explicit two-step confirm, mirroring the existing "Firm week" pattern. A11Y-001: `FocusMode` gets real focus-in-on-open / focus-restore-on-close, captured at render time (not in an effect, which raced `FocusCard`'s own child-effect autofocus) — plus an unplanned but necessary fix: the overlay is now portaled to `document.body` (mirrors `MobileNav`), because `AppShellChrome`'s `isolate` root was silently trapping the fixed z-50 overlay below `TopBar`'s z-40 in real paint order, making its close button unclickable (confirmed live via `document.elementFromPoint`; Escape-close was unaffected). A11Y-002: `role="grid"` + `aria-rowcount`/`aria-colcount` added to the inventory-flow desktop grid's scroll container (rows/gridcells were already correctly roled from prior tranches — only the grid root was missing). New `tests/e2e/meeting.spec.ts` + extended `procurement-focus.spec.ts`/`inventory-flow-smoke.spec.ts`, all `@mocked` chromium green (12/12). tsc 0, eslint 0, vitest 872/872.
- `docs/portal-os/tranches/122-meeting-lock-language.md` — Tom-approved (2026-07-03, DR-018 P1 batch; renumbered 117→122; stacked on `portal-os/tranche-121`): "firm"→"lock" lexicon on `/planning/meeting`'s action/status strings (button labels, banner headlines, disabled-reason text — 6+ occurrences) plus the one-line `/planning` overview cadence-block fix that closes the exact "Firm week" vs "lock the week" cross-page inconsistency COPY-001 names (COPY-001). `cadence.ts` 403/503 errors mapped to plain operator copy via a new shared `mapCadenceMutationErrorMessage()` helper — "break-glass" jargon removed from both `useFirmWeek` and `useGenerateDrafts` (COPY-002/COPY-005). "Try again" retry button on the generate-drafts error banner (INTER-004). Focus ring added to the lock-confirm Cancel button (A11Y-003). `CommitmentPanel` gained an optional `footer` prop so the commitment card and the lock CTA render as one visually contiguous card instead of two divider-split sections (VISUAL-002). `item_name ?? "Unknown product"` replaces a raw `item_id` fallback (COPY-008); `sr-only` "Tea"/"Matcha" labels added next to the color-only track dot (A11Y-009). Extended `tests/e2e/meeting.spec.ts` +2 tests (5/5 green). tsc 0, eslint 0, vitest 872/872. portal-tranche-verifier PASS on the second pass (first pass caught 3 manifest/copy-sweep gaps, all fixed).
- `docs/portal-os/tranches/123-production-plan-board-clarity.md` — Tom-approved (2026-07-03, DR-018 P1 batch; renumbered 118→123; stacked on `portal-os/tranche-122`): `is_user_modified?: boolean` added to the portal's `ProductionPlanRow` type (backend companion in `gt-factory-os`, degrades gracefully). `/planning/production-plan`: "Report Production"→"Open Production Report" (COPY-003); new "Completed" chip on done rows — was color/icon-only (COPY-007); raw `item_id` fallback→`"this item"` in both the Cancel-plan and Delete-record confirm modals (COPY-008); "Add from recommendations"→"Add from Recommendations", both occurrences (COPY-009); draft chip upgraded to an info-toned variant (VISUAL-006) plus a "Confirm via Weekly Meeting → Lock" link (FLOW-007) plus an "Edited" badge gated on `is_user_modified` (INTER-002); new non-dismissible draft-review banner in the command header when any draft rows exist, linking to Weekly Meeting (FLOW-007); `Toast` close button gets a focus-visible ring (A11Y-004); `EditModal` + `EditNoteModal` both gain a dirty-close guard (Escape/backdrop-click/Cancel → inline "Discard unsaved changes? / Keep editing" instead of silent loss, INTER-007) and a `Loader2` spinner beside "Saving…" matching `ManualAddModal`'s existing pattern (INTER-008). New `tests/e2e/production-plan-board.spec.ts` (3/3 green). tsc 0, eslint 0, vitest 872/872. portal-tranche-verifier PASS on the first pass.
- `docs/portal-os/tranches/124-hebrew-surfaces-precision.md` — Tom-approved (2026-07-03, DR-018 P1 batch; renumbered 119→124; stacked on `portal-os/tranche-123`): `PlacementRow`'s "בצע הזמנה" gets a proactive `canPlace` disable + Hebrew tooltip instead of post-click-only validation (INTER-003); a blank confirmed-arrival-date now adds an explicit warning sentence to the place confirm dialog (INTER-005); `ConfirmDialog` gains an optional `srFallbackDescription` so Hebrew-surface callers never leak an English a11y fallback (COPY-010, used by `PlacementRow`). `/purchase-orders/placement-queue`: empty-state honesty line ("if you know orders were approved and don't appear here, contact planning" — FLOW-004); new overdue-aging banner above the list (FLOW-006). `/planning/procurement`: supersede-session warning now names how many orders would be lost (INTER-006); `session_date` formatted via the existing `fmtDateHe` helper (COPY-006); the view-toggle (list/calendar) replaced with the shared `useRovingTabList` hook — arrow-key navigation, roving tabindex, mouse click all working (A11Y-006). `FocusMode.tsx`: all 4 literal RTL arrow glyphs wrapped in `aria-hidden` spans (A11Y-005). **Deliberately not implemented** (documented, not silently dropped): FLOW-005's session-context header row ("this session was created from the week locked on {date} · {X} batches") — the `PurchaseSession` type has no field for either the locked-week date or a batch count; implementing it would mean fabricating data or a new backend endpoint, both out of this tranche's scope. New `tests/e2e/placement-queue.spec.ts` (4/4) + extended `procurement-focus.spec.ts` (+2 tests). tsc 0, eslint 0, vitest 873/873 (PlacementRow.test.tsx's sole pre-existing test rewritten to match the new proactive-disable UX + 1 net-new test, judged a faithful strengthening not a coverage loss). portal-tranche-verifier PASS on the first pass.
- `docs/portal-os/tranches/125-validation-surfaces-orientation.md` — Tom-approved (2026-07-03, DR-018 P1/P2 batch, last tranche of the batch; renumbered 120→125; stacked on `portal-os/tranche-124`): `risk.ts` gains `CELL_TIER_LABEL` (6-key human-readable map) + a `cellTierLabel()` resolver covering both the 5-tier and coarser `DayCellTier` fallback values, so `DayCell.tsx`'s aria-label never leaks a raw snake_case enum like `critical_stockout` again (A11Y-007); non-working day cells get `tabIndex={-1}` — they're no longer real Tab stops with nothing behind them (A11Y-008). `/planning` overview's "planning pipeline" section retitled "Engine diagnostic" with a one-line corridor disclaimer clarifying it's an engine-health monitor, not a second order path — the cadence block below (the real corridor) is untouched (FLOW-003). `/planning/production-simulation`'s containment banner reworded from all-negative framing ("does not... is not...") to positive framing ("Use this to check material needs before committing") (COPY-004). `/planning/inventory-flow`'s `metadata.description` AND `InventoryFlowClient.tsx`'s visible `WorkflowHeader` caption both gain a corridor-role sentence — the manifest was extended mid-tranche to the client component once it was confirmed the page.tsx metadata alone is an invisible `<meta>` tag, not on-page text (FLOW-009). New `risk.test.ts` (4 cases) + 3 new `tests/e2e/inventory-flow-smoke.spec.ts` tests (10/10 green). tsc 0, eslint 0, vitest 877/877. portal-tranche-verifier PASS on the first pass.
- `docs/portal-os/tranches/126-ux-release-gate-p0-fastfollow.md` — Tom-directed (2026-07-04, full `/ux-release-gate` run against the DR-018 batch tip + `factory-os-governor` CONDITIONAL_SHIP verdict; stacked on `portal-os/tranche-125`): fixes the 6 pre-merge-blocking findings the gate named. Two batch-introduced regressions: the meeting `CadenceRail` STEPS label still read "Firm" beside the "Lock week" button (tranche 122's copy sweep missed this array entry) → relabeled "Lock" (`key: "firm"` untouched); the production-plan draft-review banner (tranche 123's FLOW-007) told operators to "return to the planning chat" — no such surface exists → replaced with a real `/planning` link. Four pre-existing P0s the gate surfaced (not introduced by DR-018, fixed as an immediate fast-follow given severity): production-plan board crashed (`data!.rows` non-null assertion) on any malformed API response → `data?.rows ?? []`; placement-queue crashed the same way AND would have leaked a raw English JS exception onto the Hebrew-only bookkeeper surface → `.sort()` guarded + new `ApiError` class so the error UI never trusts an arbitrary thrown error's message; production-plan's today-strip leaked a raw `item_id` UUID as the product label → "Unnamed product" fallback; `DayCell`'s day-detail popover was unreachable by keyboard (Radix `Popover.Trigger asChild` only merges `onClick` onto the cell `div`) → `onKeyDown` added for Enter/Space. New regression-guard tests in `production-plan-board.spec.ts`, `placement-queue.spec.ts`, and `inventory-flow-smoke.spec.ts` (T09) lock in the three bug fixes; existing "Firm"→"Lock" test locators updated to match the intentional rename. ~25 additional P1/P2 gate findings triaged as follow-up, not dropped (see tranche doc). tsc 0, eslint 0, vitest 877/877, playwright 25/25 (21 pre-existing + 4 new). portal-tranche-verifier PASS on the second pass (first pass caught a missing registry entry, this one).
- `docs/portal-os/tranches/127-raw-id-leak-fastfollow.md` — Tom-directed (2026-07-04, second `/ux-release-gate` run against `main` post-merge, executed autonomously per Tom's "run end-to-end with boundaries you define" instruction; `factory-os-governor` HOLD verdict authorized this fast-follow in-lane, no Tom approval required): fixes 3 fresh P0 raw-internal-ID leaks the re-audit surfaced in files tranches 121-126 never touched — meeting `BatchChip`'s pack-breakdown fallback (visible span + title/aria-label, both `?? item_id` → `"Unknown product"`), production-plan's `AddFromRecommendationsModal` (list item + selection footer, `?? item_id` → `"Unnamed product"`), and production-simulation's primary `ProductSelector` (`displayName` fallback chain ended in a raw `bom_head_id` UUID rendered as dropdown option text → `"Unknown product"`). Bundled opportunistically per governor's recommendation: 3 related P1s (redundant `?? item_id` intermediate-fallback step removed from 3 production-simulation date-range views) and 1 Hebrew-doctrine P1 (`/planning/procurement`'s `jsonOrThrow` prepended a raw English `reason_code` enum to Hebrew error text — same violation class as tranche 126's placement-queue fix, mirrored here). New `tests/e2e/procurement.spec.ts` regression test locks in the Hebrew-doctrine fix. All 6 DR-018 + tranche 126 fixes independently re-confirmed still correct with no regressions. Remaining gate findings (NaN% demand-coverage, Simulate tooltip, procurement's inline `alertdialog` misuse, ~7 a11y items, ~6 P2 polish items) triaged as follow-up, not dropped. tsc 0, eslint 0, vitest 877/877, playwright 27/28 (1 pre-existing unrelated flake, independently confirmed via isolated `main` worktree run — not caused by this diff). portal-tranche-verifier PASS on the second pass (first pass caught a missing registry entry and a premature "verified: PASS" frontmatter line written before the verifier actually ran — both corrected).
- `docs/portal-os/tranches/128-decision-board-true-margin.md` — Tom-approved (2026-07-15, true-gross-profit corridor `gt-factory-os/SPEC.md` §T4): `/admin/decision-board` moves off in-browser material-margin math onto the server CM2 read model (`GET /api/unit-economics` → v_fg_unit_economics, migrations 0282/0283) — realized Shopify price first, operating costs applied server-side, target-price column + CM2 waterfall in the Inspector, price-anomaly/stale-sync flags, and a new in-page "Operating costs" drawer (`PATCH /api/economics/operating-costs`, all-or-nothing + audit log). Old client derivation block deleted (SPEC §V.1: zero money math in the browser). Locked testids preserved. tsc 0, eslint 0, vitest 886/886; e2e execution pending live API with 0282/0283 applied.
- `docs/portal-os/tranches/129-decision-board-beauty-pass.md` — Tom-directed (2026-07-15, "תעשה את הדף הזה יפייפה" via /frontend-design + /ui-ux-pro-max + /deepen): visual elevation of `/admin/decision-board` inside the Operational Precision system — signature factory-floor **hazard striping on the portfolio map's loss zone** (+ "LOSS ZONE" caption), orchestrated staggered page-load reveal, larger tabular numerals on vitals, proportional mini-bars on the Inspector's CM2 waterfall, decision-color row accent bars, drawer slide-in + ≥44px switch hit-areas, bubble-label collision guard. Closes gate conditions C1 (saved-confirmation chip), C2 (mobile inspector scrollIntoView), C5 (page-scoped prefers-reduced-motion gate in new `decision-board.css` — globals untouched). tsc 0, eslint 0, vitest 886/886, e2e 1/1; self-critique screenshot loop caught + fixed a label collision pre-ship.
- `docs/portal-os/tranches/130-procurement-corridor-discard-and-truth.md` — Tom-directed (2026-07-16, `/ux-release-gate` procurement-corridor HOLD run wf_8a93229b-763 + Tom chat directives): procurement-corridor control + hand-off truth. `/purchase-orders/placement-queue` gains **discard-with-reason** (gate FLOW-5/INTER-6, ranked #2; solves the 6-zombie `APPROVED_TO_ORDER` backlog seen 2026-07-16) — new `useCancelOrder()` reads existing notes, appends a dated Hebrew reason (preset picker כבר לא נדרש/כפילות/הוזמן בערוץ אחר/הספק לא זמין/מחיר-תנאים/אחר + free text), `PATCH`es notes then `POST`s the existing cancel endpoint (backend already admits `APPROVED_TO_ORDER→CANCELLED` per migration 0258 — frontend-only); `PlacementRow` header split into sibling expand+discard buttons, inline `tone:"danger"` confirm, disabled until a reason is chosen; page gets a durable "removed from queue" banner via `onCancelled`. `/planning/procurement` hand-off truth (gate FLOW-6/COPY-5, ranked #3): `FocusCard` place CTA "סמן כבוצע — צור הזמנה" → **"העבר לביצוע רכש"** (Tom's phrase — it hands to the office-manager queue, not a completion), `placed` status "בוצע" → "הועבר לביצוע", approve CTA de-conflated to "הפק מסמך הזמנה", placed banner names the placement-queue destination; `ActionList` `placed` label mirrored. New `PlacementRow.test.tsx` discard-guard test. tsc 0, vitest 65/65 (procurement + purchase-orders), playwright @mocked 22/22. Deliberately deferred to tranche 131: filter/sort, defer/postpone, delete-from-session, `/purchase-orders` enum/UUID leaks, recount→inbox. FLOW-8 (P0 role-lattice) left for Tom's written decision — editing it touches a locked decision.
- `docs/portal-os/tranches/131-procurement-corridor-filter-sort-and-flow8.md` — Tom-directed (2026-07-16, same chat as tranche 130; closes the two open decisions from that tranche): **FLOW-8 closed** — Tom picked option A (Doreen = `planner`, already true live in DB); the remaining contradiction was `cockpit.ts`'s placement-queue home tile carrying a dead `he` field that `ROLE_COCKPIT` (lang="he" only for `viewer`) could never render — deleted, zero behavior change, `cockpit.test.ts` (23 tests) still green. **Defer vs. cancel-with-reason** (session POs): Tom confirmed semantics — defer auto-resurfaces next session via the engine's live netting recompute (existing "דלג", unchanged); cancel-with-reason is the deliberate audited alternative — `FocusCard` gains a "בטל עם סיבה" panel (preset reasons + free text) that populates `skip_reason`, a column/write-path `handleSkipPo` already had server-side but the frontend never collected. `ActionList` gains supplier + tier filter and 3-way sort (order-by-date/amount/supplier) applied *within* the existing must_today/can_wait/handled buckets — the "X חייב לצאת היום" risk banner is deliberately computed from the FULL unfiltered session so an active filter can never visually shrink reported risk. `/purchase-orders/placement-queue` gains the matching supplier filter + sort, same full-queue-always overdue-count rule. tsc 0, vitest 91/91 (7 new: FocusCard F6, ActionList L6/L7), playwright @mocked 14/14 (meeting + placement-queue + procurement-focus, real dev server — confirms no regression from the header/panel restructuring). Deliberately deferred: delete-from-session, bulk multi-select, `/purchase-orders` raw-enum/UUID-leak P2s.
- `docs/portal-os/tranches/132-procurement-triage-decision-grade.md` — Tom-directed (2026-07-16 chat, procurement-corridor audit + rebuild; stacked on 130–131, pairs with backend 0284/0285): the triage stage of `/planning/procurement` rebuilt around the planner's three real decisions — send today / can wait / **count first** — after the audit showed ~97% of session POs since May landed in one `urgent` bucket (floor-date tier). `decision.ts` v2 classifies from per-line `coverage_trace` shortage math (est. zero-date vs arrival-if-ordered-today, quantified "חוסר צפוי ~N ימים", explicit "אפשר להמתין עד DD/MM", v1 date/tier fallback for old sessions); recount chips (engine-reported count ages, link to `/stock/physical-count`) + "דורש ספירה" filter; new `IntegrityStrip` (drift, count freshness, forecast age+coverage gap, firmed weeks, warnings as chips) replaces the banner stack; warning payload mapped INLINE to affected rows (live RAW-NANA double-buy); search by supplier OR item; tier/`מוצע` badge noise removed; full-session summary invariant kept. New `_lib/integrity.ts` + `_lib/session-warnings.ts` (+tests), `IntegrityStrip.tsx` (+test); `types.ts` exposes `demand_model_version`/`firmed_window`/`input_integrity`. tsc 0, vitest 914/914 (94 procurement-scope), playwright procurement spec 2/3 (third fails identically on base — sandbox auth artifact), live 0284 off_cycle session verified end-to-end. Deliberately deferred: SQL tier revision (ADR'd in skill refs), buffer tuning, ₪-trend vs previous session.
- `docs/portal-os/tranches/133-procurement-weekly-meeting-followups.md` — Tom-directed (2026-07-16, "רוץ עם ההמלצות שלך" on 4 questions asked back about the weekly-meeting screen; same corridor as 132): **recount-loop closure** — `IntegrityStrip` gains a "רענון המלצות" action, shown only when the input actually looks untrustworthy (drift / stale counts / `stale_stock_input`), reusing the existing supersede-confirm mutation; **click-to-fix warning chips** — every warning chip (strip-level and, for the double-buy case, row-level in `ActionList`) now resolves a real `href` — PO-hygiene codes → `/purchase-orders/[po_id]`, `components_without_supplier` → the affected component/item master-data record (needs backend companion below), degrading to a plain tooltip when no safe target resolves; **mobile collapse** — the strip is a one-line tap-to-expand summary below `sm`, full row unchanged on desktop; **`--fg-faint` contrast** — was 2.01:1/2.90:1 (light/dark) against `--bg`, recomputed to 4.95:1/4.71:1 (both themes, app-wide token, math verified via a small script not eyeballed). Backend companion `gt-factory-os` migration 0286 (`components_without_supplier` warning now carries `is_item` — additive field, `_ps_orders` already had the column; applied to prod, verified live). tsc 0, vitest 926/926 (10 new). Real dev server + Playwright screenshots, both themes/viewports, collapsed+expanded — confirmed live, not just unit-tested.
- `docs/portal-os/tranches/134-report-return-to-plan-card.md` — Tom-directed (2026-07-21, "תתקן את זה מקצה לקצה"): the report-from-plan round trip now returns the operator to the exact card they left. `planBoardReturnHref` (report-helpers, week math imported from the board's own helpers — Sunday-first can't drift) builds `?week=<plan week>&focus_plan=<plan id>`; four report-form links deep-link (success panel, pre-submit banner, both plan-conflict paths); the board consumes `?focus_plan=` — scrolls the card into view on both axes, flashes a ~2.5s `ring-accent` ring (`data-return-focus`), strips the param via replaceState, skips the TODAY auto-center while pending, falls back to it on a stale id. tsc 0; report-helpers 22/22 (6 new); board e2e 5/5 (@mocked, +1 new); full submit→back→focused-card journey proven live via stubbed dev-server run with screenshots. The 7 Recipe-Health-family fixture date-drift failures (base-reproduced, then confirmed red in CI too — portal-pr-guard was blocked repo-wide from 2026-07-19) fixed in the same tranche: fixtures now compute `FRESH_AT` relative to now instead of pinning 2026-04 dates that crossed the 90-day PRICE_AGE_WARN_DAYS policy; suite back to 932/932.
- `docs/portal-os/tranches/135-ux-gate-p0-and-s-batch.md` — Tom-approved (2026-07-21 "צא לדרך", from the deepened `/ux-release-gate` re-run, brain PR #48 — HOLD, 1 P0): closes **INT-P0-1** — double-tap on the `IntegrityStrip` "רענון המלצות" bypassed the supersede confirm (first tap armed the header confirm with zero feedback at the still-enabled button; second tap fell through `handleStart`'s guard and silently replaced the open session) — the strip now receives `refreshConfirming`, disables and reads "ממתין לאישור…" while armed, with an S7 regression test; plus the gate's 13 S-effort P1s: admin-target fix chips capability-degrade to tooltip for non-admins (FLOW-101), cancel-panel Escape containment + dirty-guard (INT-101), PlacementRow expand/cancel mutual exclusion (INT-102), "נקה סינון" touch target (INT-103), mobile "· רענן" hint in the collapsed strip (INT-105), "~" on extrapolated can-wait dates (FLOW-104), cancel-banner "צפה בהזמנה" link (FLOW-105), FocusMode corridor vocabulary — flash "הועברה לביצוע", DoneSummary "הועברו לביצוע" (COPY-101), unified XCircle+label cancel trigger (VIS-101), labeled cancel select + focus-move + aria-expanded (A11Y-101/102), queue filter live result-count region (A11Y-103), FocusCard thead/qty off `--fg-subtle` (A11Y-104), refresh spinner motion-reduce (A11Y-201). tsc 0, vitest 935/935 post-rebase on #174 (3 new tests; the recipe-health fixture time bomb was independently fixed by tranche-134/#174 — this tranche's duplicate fix dropped in rebase), playwright @mocked 14/14 (procurement-focus + placement-queue + procurement + meeting, dev-shim). Deferred to 136: FLOW-102/103, INT-104, COPY-102 (Tom preset decision), FLOW-9, VISUAL-12, A11Y-2.
- `docs/portal-os/lean-nav-audit-2026-07-22.DRAFT.md` — DRAFT audit (2026-07-22, factory-mapping v3 rollout, Tom's lean directive): how nav is gated (NAV_MANIFEST → min_role/capability → middleware → cockpit), real-people→role mismatches (Dorin-as-planner orphans the Hebrew viewer cockpit; Dennis/Maxim share one operator cockpit, no accounts yet), per-role minimal destination maps, top-5 fold candidates. Read-only; feeds DRAFT tranches 136-138.
- `docs/portal-os/tranches/136-today-board.md` — mapping-v3 Q6 (Tom, 2026-07-22, approval recorded 2026-07-23): 3-tab Today board (Yesterday/Today/Tomorrow, English per policy) mounted inside existing `/home` for operator/planner/admin (viewer cockpit unchanged), read-only v1, gated `role !== "viewer"`. New pure `_lib/today-board.ts` (plan-vs-actual join via `completed_submission_id`↔`submission_id` cross-check with no-report flag leading Yesterday, unmatched-submission handling, credits date filter, arrivals bucketing incl. overdue, tomorrow READY/SHORT tier from the same production-aware `shortfall_qty_with_production` Inventory Flow uses). Reuses `useRovingTabList`, `usePlans`, `useInventoryFlow` verbatim; no new endpoints. Named gaps G2 (delivery exceptions), G3 (route/departure), G4 (aggregate-READY per item) render honest permanent notes, never fabricated. tsc 0, eslint 0, vitest 968/968 (24 new), playwright @mocked chromium 32/32 (7 new) + mobile-safari screenshot pass (1/1, visually inspected).
- `docs/portal-os/tranches/137-receipts-against-po.md` — mapping-v3 Q10 (Tom, 2026-07-22): `/stock/receipts` door mode for Dennis. Landing leads with "Expected today", manual/no-PO track visually demoted (not hidden), PO-ledger progress detail collapses by default behind one disclosure toggle for `role==="operator"`, ≥44px per-line touch targets. Added the symmetric counterpart to the existing over-receipt confirm: short-receipt (received < open_qty) pre-submit summary + success-panel delta naming "PO stays open for the rest" — previously silent. Dennis's access (role gate) and the express-receive confirm (tranche 086) verified already-satisfied, no code change. Presentation-only, no backend/contract change. tsc 0, eslint 0, vitest 944/944 (9 new), playwright @mocked chromium 25/25 (2 new) + mobile-safari screenshot pass (1/1).
- `docs/portal-os/tranches/138-lean-nav.md` — Tom-approved D1+D2 (2026-07-23, base commit `75cf8ad`): per-role nav pruning from the lean-nav audit, **nav-visibility only — zero route deletion, zero middleware change**. New `roles?: Role[]` exact allow-list on `NavItem` (overrides the `min_role` floor) + shared `navItemAllowsRole` applied in SideNav/TopBar/CommandPalette; `placement:"command"` folds production-simulation + blockers to ⌘K/deep-link (kept in `NAV_MANIFEST`); min_role viewer→planner on `/planning` overview + forecast; `/credit-tracking` + `/stock/movement-log` scoped out of the operator rail; D2 `isCapabilityPermanentlyUnreachable` hides the viewer's 4 permanently-locked stock forms + `/me/activity` (attainable rows keep the subdued padlock); operator cockpit stock group leads with Production Report + Goods receipt + Physical count. Every folded URL still loads directly (proven per-role) and stays ⌘K-reachable. Rails: operator ~14→9, viewer ~13→6, planner/admin lose the 2 diagnostic folds. tsc 0, eslint 0, vitest 982/982 (968 + 14 new per-role visible-row + never-grantable-rule tests), playwright @mocked chromium 10/10 (new `lean-nav.spec.ts`: absent-from-sidebar + loads-directly + ⌘K-finds-it + 4 role screenshots). baseline.json re-anchored (kind=baseline-update). nav_integrity stays 10/10 — a qualitative lean pass, no numeric delta.
- `docs/portal-os/tranches/139-meeting-tuning-cockpit.md` — implemented (pending merge): 2026-07-23 gate P0 batch — BatchTuneDialog (pack-split editing with live liters meter, everywhere), weekly-meeting = tuning cockpit (W2 chips + W1 inline board), backend PATCH pack_manifest (companion gt-factory-os PR), COPY-001/002/003/004 + VIS-001/002/003 P0s, 10 S-effort P1s. 947/947 vitest.
- `docs/portal-os/tranches/140-raw-material-first-procurement.md` — in progress: raw-material-first procurement + placement-queue. Material = hero, supplier secondary with click-to-call, materials clustered by supplier, switch-to-next-candidate-supplier with OPTIONAL reason, no-alternative → return-to-planner. Session side wires backend migration 0288 (handleRerouteLine); placement-queue side wires fn_switch_po_supplier (companion gt-factory-os PR). New shared components src/components/purchase/{SupplierCallLink,SwitchSupplierControl,CandidateSupplierList}.
- `docs/portal-os/tranches/141-production-order-picking.md` — in progress: production order picking ("מעגל פקודת הייצור", Tom 2026-07-24, brain PR #62 + spec `2026-07-24-production-order-picking-design.md`). New `(production)` route group: `/production` = operator-landing today's run list (ordered make-tank → fill-product), stage-aware picking screen (TANK liquids / PACK packaging / SINGLE both) with tap-per-row prefilled editable BOM quantities, "Done collecting" resolve-gate → `PICK_CONSUMPTION` at confirm (physical-truth-wins flags: shortage / excess / not-taken), active-run Add/Return deltas, unplanned-run create (tagged, flagged, non-blocking). Simple English only, word-poor, touch-first (≥44px / `.btn-lg`/h-14), `floor_name ?? item_name` + small Hebrew secondary. API proxies over `proxyRequest` to backend `production-runs` endpoints (companion gt-factory-os, migration 0289, same branch). Nav manifest + operator cockpit `primaryHref`→`/production` + `/production` middleware prefix. Report reshape + QC + per-date cutover + floor-name backfill deferred to tranche 142.
- `docs/portal-os/tranches/142-production-run-report.md` — in progress: production run end-of-run report (S3 follow-up to 141; brain PR #62 merged = approval). New `/production/runs/[run_id]/report` — output + scrap + optional QC (Brix/pH/sample/note), posts OUTPUT only via existing backend report endpoint, moves run→REPORTED; adds a "Report production" CTA on the pick-confirm success screen (closes tranche-141 INTER-006). Per-date cutover + floor_name backfill deferred to tranche 143.
- `docs/portal-os/tranches/145-ux-gate-iteration-1.md` — in progress: production picking rollout Phase 4. First of two post-launch UX polish iterations on the `/production` corridor. Ran the full 5-lens `/ux-release-gate` panel (interaction, visual, flow, accessibility) against real screenshots; fixed every P0/P1 (IN_PRODUCTION rows now disabled; UnplannedRunDialog X guarded; AddMaterialControl save gated; EditQtySheet floor_name subtitle; ReportForm back→/production + server-confirmed success values; 3 missing form labels; search-results live region; DoneConfirm focus trap) + cheap P2/P3 (tooltips, empty-BOM AddMaterial gate, QC "Close" copy, stepper icon/UoM/textarea consistency, aria-pressed, success-copy announcement, fg-subtle→fg-muted contrast at 2 call sites). Deferred: "תה שחור" floor_name (item data → Phase 6), amber IN_PRODUCTION banner (kept — matches status tone), full report-audit endpoint (ARCH/backend).
- `docs/portal-os/tranches/143-production-cutover.md` — in progress: production picking rollout Phase 2 (rollout plan `gt-factory-os-production-brain/docs/plans/2026-07-24-production-picking-rollout.md`). The 7 exact cutover touch-points repointing `/stock/production-actual` links/nav/tiles to `/production` (nav manifest, dashboard quick-action, home cockpit tile, production-plan page + ProductionJobCard, plan-recommendations page, inventory-flow PlannedItemSection); old submission-detail history link stays on the legacy page. `floor_name` (migration 0296) rendered big on `PickRow` with Hebrew `component_name` as small fallback. Old route stays live at its direct URL for 30 days.
- `docs/portal-os/tranches/146-ux-gate-iteration-2.md` — in progress: production picking rollout Phase 5 (Gate 5). Re-ran the full 5-lens `/ux-release-gate` against the tranche-145 build merged to main; all 15 iteration-1 fixes verified PASS by all lenses. Closes the last **2 residual P1s** the iteration-1 gate missed: INTER-006 (AddMaterialControl correction dropdown showed Hebrew `component_name` while PickRow shows Latin `floor_name` → `floor_name ?? component_name`) and INTER-007 (PickRow confirm/edit stayed tappable during `confirm.isPending` → `disabled={terminal || committed || confirm.isPending}`). Plus a tight set of cheap, zero-risk P2 one-liners done while in-file (A11Y EditQtySheet UOM `fg-subtle`→`fg-muted`; steppers disabled during in-flight mutation across UnplannedRunDialog/AddMaterialControl/ReportForm; DoneBar blocked-button title tooltip; PickList + ReportForm live regions announce "Saving…"). Remaining P2s documented as a bounded backlog (globals.css `.reveal` motion-reduce = frozen token change; stale-BOM two-step; report-guard direct-URL; etc.). Exit = ZERO P0/P1 on the /production corridor.
- `docs/portal-os/tranches/147-report-production-flow.md` — verified: report-production flow (Tom 2026-07-24, in writing). Fixes the plan-card → report dead-end (`reportHref` now carries the plan's own `plan_date` + `plan_id` + `report=1`; `/production` reads `?date=`/`?plan=` and forwards to the report when a plan resolves to exactly one reportable run), makes back-dated reporting first-class (day picker, a report CTA on every reportable run card, backend accepts a `PLANNED` run), and pre-fills the report's output with the planned quantity (derived, labelled as the plan). Companion backend change on the same branch moves RM/PKG consumption from pick-time to **report-time** (migration 0297, comment-only): collecting writes `production_run_pick` rows only, corrections are signed pick rows, and the report nets them per `(source, component_id)` and posts capped `PICK_CONSUMPTION` in the same transaction as `PRODUCTION_OUTPUT` — so a run cancelled after collecting has nothing to reverse. Full 5-lens `/ux-release-gate` run: zero P0 remaining (two-step confirm on the stock-moving submit; TANK runs never offered a report; terminal runs answer the pick list 200 instead of a fake network error). Two P1 copy items held for Tom (standard lexicon vs this surface's simple-English-for-Denis mandate). Gate record: `gt-factory-os-production-brain/docs/phase8/dry-runs/2026-07-24-ux-release-gate-tranche-147.md`.
- `docs/portal-os/tranches/148-production-qty-clip-and-tablet-verify.md` — verified: Tom reported live that quantities sometimes clip while collecting on `/production`; root-caused to `fmtNumStr()` not enforcing its documented 4dp rule, letting an unrounded `numeric(24,8)` value overflow `PickRow`'s fixed-width number button. Fixed at the source (rounds to 4dp, propagates app-wide) + `PickRow`/`EditQtySheet` layout defense-in-depth. Also closes the tablet (768px) verification gap on `/production` (145/146), `/home` (136), and `/stock/receipts` (137) — each was only screenshot-verified at 390px/1440px, never at Tailwind's `md` breakpoint. New tablet Playwright project + 3 `tablet-*.spec.ts` specs. Independently found and fixed a dead-URL bug in `mobile-operator-forms-smoke.spec.ts` (`/ops/stock/...` 404s instead of the real `/stock/...` routes, invisible because it's WebKit-only). tsc 0, eslint 0, vitest 1089/1089, playwright chromium 13/13.
- `docs/portal-os/tranches/149-counting-surface-audit-fixes.md` — verified: 5-lens audit (interaction/accessibility/visual/flow/content) of `/stock/physical-count` + `/inventory/bulk-count` — Denis's + Maxim's Thursday counting surfaces, never previously given a mobile/tablet pass. Fixes 2 P0s (FLOW-015: bulk-count regenerated its idempotency key on every retry, risking a double-posted count on a network drop; COPY-005: bulk-count rendered Hebrew section headers on a non-Hebrew-whitelisted surface via `groups.ts`'s Hebrew-first default — added an additive `preferEnglish` param, default `false`, zero behavior change elsewhere) plus a large batch of P1s (touch targets to the §7 32px floor, safe-area-inset on both pages' sticky bars, inline qty-validation error, signed-delta formatting, terminal-vs-retryable error split, snapshot-expiry warning, several a11y fixes: focus trap, always-mounted live region, role=alert, aria-expanded/label) and P2 polish (jargon sweep, emoji-icon removal, cross-links between the two pages). Full backlog of deferred items (combobox keyboard nav, resume-after-interruption, pending-submissions visibility, a backend-blocked ARCH_REQUIRED planning-signal gap) documented in the tranche doc with exact specs. tsc 0, eslint 0, vitest 1089/1089, live Chromium smoke (390px+768px, both pages) zero JS errors, English section labels confirmed live.
- `docs/portal-os/tranches/150-partial-placement.md` — in progress: **partial placement** of a purchase order (Tom 2026-07-26, `/ck:grill`; backend companion `gt-factory-os` PR #187 / migration `0298_partial_placement_split`, already merged + live in prod). The supplier has some of the order but not all: `/purchase-orders/placement-queue` gains a **three-state control per line** — `✅ הוזמן במלואו` (default) / `➗ הוזמן חלקית` + quantity / `❌ לא הוזמן` — so the common "he had everything" path stays one tap on `בצע הזמנה` and friction appears only when reality is messy. The unplaced remainder is sent as `unplaced_lines` + a required `split_reason`, and the backend splits it onto a **sibling PO in `APPROVED_TO_ORDER`** carrying the dated Hebrew reason + `split_from_po_id`; the sibling re-enters this same queue where the existing `SwitchSupplierControl` (tranche 140) retargets it to another supplier — no new supplier-picking UI. Not just convenience: placing a whole PO flips every line to `OPEN`, which the netting `po_supply` CTE counts as incoming supply, so a line the supplier never had became **phantom supply masking a real shortage** (the PKG-BOTTLE-1L failure class); a sibling in `APPROVED_TO_ORDER` is correctly not counted. Confirm dialog **itemises** placed-vs-split — DR-019's exact P0, which the earlier attempt at this UI (PR #164) shipped and never landed. One reason per split (a split is one supplier conversation), MOQ violations warn rather than block, and all-lines-❌ is refused client-side and routed to the existing discard-with-reason path.
- `docs/portal-os/tranches/151-add-supplier-unblock.md` — Tom, 2026-07-26 live report ("זה לא נותן לי להקים ספק", screenshot showed a bare "Validation failed."): the Add-supplier dialog **could never create a supplier**. `POST /api/v1/mutations/suppliers` requires `idempotency_key`, and the shared quick-create helper posted the form values verbatim without one — so every submit 422'd regardless of input. Scope-checked: only the suppliers endpoint requires that field, so the component/item/supplier-item quick-creates were unaffected (matching that Tom only ever hit it on supplier). Two further defects fixed in the same pass: `shared.ts` already received the server's `issues` array but threw it away in favour of the bare `"Validation failed."` fallback, so the dialog could not say which field was wrong; and the STATUS select was inert — `SupplierCreateRequestSchema` has no `status` field and the non-strict `z.object` silently strips it, so every supplier landed `ACTIVE` (DB default) whatever was picked, now replaced with an honest note. The idempotency key is held in a ref so a retry after a network error reuses it rather than creating twice. Client-only; no contract or data change. Also reported but deliberately not cleaned: junk `SUP-MANUALPG-*`/`SUP-MPGS-*` test suppliers in prod and two separate "Amit" records (`SUP-010`, `SUP-049`).
- `docs/portal-os/tranches/152-consumption-summary-before-finish.md` — Tom, 2026-07-27: reporting a run **without collecting consumed nothing at all** — three live runs on 2026-07-26/27 produced 179 finished units and moved zero raw material or packaging, because `postRunConsumption` returned early on an empty pick list while the run card offers "Report" beside "Collect materials" with no dependency between them. Backend now reconciles per component (collected wins where it exists; the recipe at the **actual** reported output fills the rest) and the portal shows that reconciliation in the confirm step that already existed — no new page, no extra tap. Each row states whether its number was measured or derived plus `collected N · recipe says M`. A material the projection says is empty offers a tick to post it anyway and let the balance go below zero (Tom: bottles standing unlabelled are a debt against a label delivery, not a phantom) — never blocking, since it has a safe default; a collected quantity a factor of two or more from the recipe, in **either** direction, blocks until the operator says what happened. 1107/1107 vitest, typecheck + lint clean.
- `docs/portal-os/tranches/153-thursday-count-list.md` — built: **the Thursday count list finally reaches `/inventory/bulk-count`** (Tom 2026-07-27: "יש לי איפה לסמן את זה בדף הרכש אבל איך זה בא לידי ביטוי בספירה?"). Audit found the chain was never connected: the purchasing page's "לספור קודם" flag is **auto**-computed (`decision.ts` ← `coverage_trace.last_count_age_days`, 0284) and linked to `/stock/physical-count` — the single-item screen — **passing no parameters**, so the operator landed on a blank form with nothing carried over; bulk-count knew nothing about any of it (every filter derives from `/api/stock`, filter state is local with no `useSearchParams`); and `v_count_first_queue` (0294) has had **zero consumers** since it was written. No manual mark existed anywhere — all 298 migrations searched for `mark_for_count`/`needs_count`/`to_count`/`count_flag`: nothing. Tom's policy when asked (auto vs auto+manual vs manual): "ידני בלבד לחומרי גלם ואריזות. ואת התוצרת מוגמרת הוא צריך לספור את כל הסוגים" → **FG counted in full every week, RM/PKG only where marked**. Adds a `thursdayList` filter (FG always passes; RM/PKG only with an open mark; **degrades to FG-only if the marks fetch fails** — a failure must never silently widen the list back to every component), a one-tap **"Thursday count · N"** chip carrying the whole walk (English, per this surface's COPY-005 non-whitelist status), a per-line **"סמן לספירה"/"מסומן לספירה"** toggle on `ActionList` (Hebrew — `/planning/procurement` is whitelisted; FG lines get no toggle), and re-points `COUNT_HREF` to `/inventory/bulk-count`. Backed by `gt-factory-os` PR #190 / migration `0299_count_marks` (append-only `count_mark_events` + `api_read.v_open_count_marks`); marks **self-clear** once a non-reversed physical count lands after the mark, so there is no reset job and nothing to sweep. tsc 0, eslint 0, vitest 1104/1104 (+6), pgTAP 8/8 against a real PG16 with the full migration chain applied. Known gap logged, not fixed: no CI in `gt-factory-os` runs pgTAP at all.
- `docs/portal-os/tranches/154-placement-queue-clarity.md` — built: **the placement queue rebuilt so it can be read at a glance** (Tom 2026-07-30: "חייבים לשפר את דף ביצוע הרכש שדורין עובדת עליו… הוא לא מובן ולא מספיק ברור לשימוש"). Executes the ranked actions from the full `/ux-release-gate` run (`PRODUCTION/docs/phase8/dry-runs/2026-07-30-ux-release-gate-placement-queue.md`, verdict HOLD: 1 P0, 32 P1, 12 P2; before/after shots under `assets/uxg-2026-07-30/`). The **P0 was a hierarchy collapse** — the supplier name printed twice per row, once as the group heading and once as the row's own primary label at the same size and weight ~10px apart, so every row read as a duplicate of its own container; it now appears exactly once and the row leads with a status chip. The page is reorganised around **the phone call**: one supplier group = one call, groups ordered by their most urgent member with that state on the header, which settles the gate's one real disagreement (flow auditor wanted a flat urgency list, interaction auditor wanted grouping kept for one-call-per-supplier batching) without losing either. Urgency became scannable — a tone rail at the RTL start edge plus a chip carrying an icon and a word (never colour alone) — and every row states its own next step, since a row needing a date used to look identical to one ready to place and the prerequisite surfaced only after the prices were typed. The expanded row now follows a real supplier call (**1 ·** send the paste-ready message, **2 ·** record what was confirmed, then arrival date and payment terms); the order document had been sitting *below* the price fields. Saving a date acknowledges itself and advances into the pricing panel instead of closing silently onto a dead end, and panels no longer auto-open — two of them had pushed the rest of the queue below the fold on a phone. Copy: planner jargon out of the schedule panel ("מידע נעול מהשרת", "סיבת סיכון", "סיבת חריגה מתאריך בטוח"), `לא הוזמן` → `לא יסופק` (the order *was* placed; that line just is not coming), Hebrew units instead of raw `KG`/`L`, internal IDs no longer standing in for item names, and the confirm dialog names the supplier and stops claiming a placed order cannot be cancelled — it can, it just costs a phone call. A11y: the blocked-submit reason is on-screen text on a focusable `aria-disabled` button rather than a `title` on an unreachable `disabled` one, and the supply outcome is a native radio group rather than three `aria-pressed` toggles. **One gate finding was wrong and is recorded as such**: VIS-102 proposed `lang="he"` to make `<input type="date">` render DD/MM/YYYY — verified against a render, it does not, because Chromium formats date inputs from the *browser's* locale and ignores the document language; the fix is a `DateEcho` printing the Israeli format beside every date input. No token, `globals.css` or `tailwind.config.ts` change; no backend change. Deliberately not closed: COPY-110 (shared cancel-reason catalogue — still needs Tom's per-role subset), INTER-108, A11Y-106 (token-level, frozen). tsc 0, eslint 0, vitest 1130/1130, playwright @mocked 56/56.
- `docs/portal-os/tranches/155-corridor-focus.md` — built: **the procurement corridor made focused, end to end, plus the Wednesday/Thursday cadence** (Tom 2026-07-30: "המסדרון צריך להיות יותר ממקד… האם יש פה משהו מבלבל או שברור לגמרי מה צריך להיות הצעד הבא?" and "the workers need to know what to focus on in each page"). Inputs: a corridor walk (`PRODUCTION/docs/phase8/dry-runs/2026-07-30-ux-flow-audit-procurement-corridor.md`) plus three per-page focus audits, unified under ponytail discipline into `2026-07-30-corridor-focus-unified-plan.md` — 33 raw findings → 24 built, 5 skipped with named re-entry conditions, 2 escalated to the backend lane. **Cadence (Tom-locked):** the meeting moved Thursday→Wednesday and procurement Sunday→Thursday; `stepForToday()` was already the single day gate, and day names in copy now derive from a new `CADENCE_DAYS` constant so the next move is one edit instead of six string hunts (`Sun–Thu capacity` is the factory work-week and was deliberately left alone). **Three real bugs the focus audits surfaced:** draft rows were counted as "unreported" in the today strip and offered "Move to tomorrow" — directly contradicting the draft banner above them, because a draft also carries `rendered_state: 'planned'`; base batches printed "Unnamed product" in that same strip while the card beside them said "Base batch · N products"; and one `firmed` survived tranche 139's lexicon replacement. **The P0** was on `/planning/procurement`: the cross-session work queue offered "בצע מול ספק" for an order that had not been approved yet, while the session's own list offered "פתח במיקוד" for the same supplier — two green buttons, no explanation, and a planner could press the first, believe the order was handled, and never approve it; the CTA now reads the real `po_status` and says "צריך אישור קודם" instead. **Focus work per page:** the production plan's teal primary was "Add production" on a morning whose whole job was reviewing drafts, so with drafts present it becomes "Review N drafts in Weekly Meeting" and the page description, banner and completion figure all state the draft situation honestly; the meeting demoted "Regenerate drafts" from a peer of "Lock week" to a link (warning-toned when hand edits would die), replaced the "At-risk check: Flow" pseudo-KPI — a destination name where the tiles beside it give numbers — with a plain link, and marks the Lock step Done once the week is locked; procurement promoted the session card and "התחל מיקוד" from sixth element to second, moved the tracking queue below the session it supports and renamed it "מעקב הזמנות בדרך ובעיכוב", added a placed/approved/to-review progress line, and hides the filter bar until a session is big enough to need it. Corridor handoffs: the supersede confirm now separates orders already safe in the placement queue from in-session approvals that will be lost (FLOW-9, open since 2026-07-16), the empty no-session state explains how to start one, the post-cancel banner routes back to procurement, and "Orders to Place" appears inside the Planning nav group so the corridor's last leg is visible in the sidebar. Corrected mid-build: MEET-305's "Edited" tooltip was dropped because `updated_at`/`updated_by_snapshot` are not on `DraftWeekRow` as the audit claimed — building it would have meant inventing a backend field. tsc 0, eslint 0, vitest 1133/1133, playwright @mocked 56/56, all three corridor pages re-rendered at both viewports.
- `docs/portal-os/tranches/156-over-supply-and-cancel-vocabulary.md` — built: **the supplier sent more than we approved, and that is now sayable** (Tom 2026-07-30: "צריך להיות אפשרות ביטול והזנה של הזמנה של יותר ממה שנקבע בעד 15% יותר ממה שנקבע בדף הרכש"). The placement row asked "all, part, or none?" — a question with no truthful answer when the supplier ships *above* the approved quantity (pack sizes, MOQ rounding, a substitution), so the office manager either placed a number she knew was wrong or stopped and phoned the planner, for a case that is completely ordinary. The middle state stops meaning "less" and becomes **"כמות אחרת"**: one input, valid anywhere in `(0, approved × 1.15]` — below approved the remainder still splits onto a sibling PO (tranche 150 untouched), exactly approved places as approved, above approved raises the line through the existing `line_qty_overrides` contract (migration 0261) with **no** split, and past +15% it is refused inline with the ceiling stated in real units and the planner named, because at that point the money and the stock projection have moved enough that it is a planning decision rather than a placement one. DR-019's rule — a confirm dialog must never hide a quantity change, the bug that sank PR #164 — now covers over-supply too: the dialog itemises each raised line, the new quantity, the percentage over, and says the cost rises. The discard-with-reason path (tranche 130) is unchanged and still the answer for "don't place this at all", including out of the over-supply state. No backend change: 0261's Zod caps nothing, so the +15% policy lives in the portal deliberately — policy that may change belongs where it changes in one line. Also closes **COPY-110**, open since the 2026-07-16 gate and previously waiting on Tom to pick a per-role subset: decided as one shared module (`src/lib/purchase/cancel-reasons.ts`) with **two named subsets** rather than one flat list — the planner dropping a *recommendation* and the office manager discarding an *approved PO* are different questions asked at different moments, and merging them would make every cancel a scan through inapplicable options — with every shared reason (`כבר לא נדרש`, `כפילות`) spelled identically in both so the audit trail finally lines up, plus `אחר` free text on both surfaces. tsc 0, eslint 0, vitest 1137/1137, playwright @mocked 56/56.
- `docs/portal-os/tranches/157-admin-user-passwords.md` — built: **the Users page can actually hand out passwords** (Tom 2026-07-31, with a screenshot of every row reading "No password set" under a red "Password control is not configured on this deployment": "שים לכולם את הסיסמאות שלהם שאראה אותן בדף המשתמשים באדמין ושאוכל לשנות בקלות משם הכל"). **The surface was not broken — it was un-provisioned.** The control shipped in PR #195 against API PR #192 / migration 0300, and 0300 is applied in production (`private_core.app_users.password_encrypted` exists), but `ADMIN_PASSWORD_DISPLAY_KEY` was never set on the Railway API service, so `getPasswordDisplayKey()` returned null and every Generate refused before it started — announcing itself by naming two environment variables, which is a correct thing for a log line to say and a useless thing to say to someone trying to give a warehouse operator a password. **The fix is in the API, not the page:** key resolution becomes explicit `ADMIN_PASSWORD_DISPLAY_KEY` → else HKDF-SHA256 over `SUPABASE_SERVICE_ROLE_KEY` under a fixed salt/info → else null. The service role key is *already* required to set a password at all — it authenticates the Auth Admin call that changes the credential — so a deployment that can set a password can always also encrypt it for display, and the state where the first half works and the second refuses was never worth having; the second secret bought nothing but an outage. HKDF is one-way, an explicit key still wins when set, and what is protected is a cache of a secret this system itself issued behind an admin-only route, not a user-chosen credential (those are bcrypt in `auth.users` and unreadable by anyone, this code included). The 503 now names the piece actually absent instead of listing two variables. **On the page:** `Type one` — an inline field for the exact value, `type="text"` on purpose because the admin is choosing something they are about to read out loud and masking it would only hide typos, refusing <6 / >72 chars and surrounding whitespace while typing against a tested mirror of the API schema (`_lib/password-rules.ts`); a toolbar `Set for the N without one` that walks the listed users sequentially — ordered `change_log`, no burst against Supabase Auth, one row's failure reported against that row while the rest complete — **skipping users who already have a password**, because "שים לכולם" reads as *everybody ends up with one*, not *everybody's changes*, and regenerating over a live password would silently lock someone out for no reason anyone asked for; and `Show all` / `Hide all`, plus reveal-as-you-go during the bulk run, because a generated value has no second chance to be read. API also gained an optional chosen password on POST (`AdminSetPasswordSchema`, 6–72, bcrypt's ceiling, whitespace refused not trimmed) and now maps a 4xx from Supabase Auth to 422 `REJECTED_BY_AUTH` rather than a 502 outage. No schema change. tsc 0, eslint 0, vitest 1143/1143 (+6). **The API suite was red on the original 0300 commit** — 5 of 6, confirmed at `HEAD~1`: every call sent `content-type: application/json` with no body, which Fastify rejects at the content-type parser before any handler runs, so T1/T2/T4/T6 answered 400 and T5 cascaded. It stayed invisible because `phase10-node-tests.yml` is `workflow_dispatch`-only and names three cogs files — this suite is in no automatic gate, and the API repo's one automatic PR check (`typecheck.yml`) runs the root tsconfig, whose `include` is `scripts/**/*.ts` and does not compile `api/` at all. Which is how a feature that was dead on the deployment shipped green. Fixed and run for real in this session against a throwaway local Postgres 16 (migration chain applied, CD-test fixtures seeded, production untouched — the suite refuses it by design and the override was not used): `admin_user_password_set` 11/11, 39/39 alongside `admin_user_update`, `password_crypto`, `supabase_auth_admin`, `shared_change_log`.
- `docs/portal-os/tranches/158-batch-plan-creation.md` — built: **the board can create the plan shape the factory actually runs** (Tom 2026-08-01, in writing: "תן לי אופצייה במסך התכנון היומי גם להוסיף ייצור כבאצ׳ שמתחלק למוצרים — כרגע יש לי רק אופציה לשים שם ייצור per item"). The board displayed base-batch rows — `item_id NULL`, `base_bom_head_id` + `pack_manifest`, the shape used for most real production days — but every creation entry point opened the per-item ManualAddModal; the system read a plan type it could not write (2026-08-01 `/ux-flow-audit`, FLOW-001/002 P0). **AddBatchModal**: base picker offering only ACTIVE BASE heads with ≥1 ACTIVE member item, tank size prefilled from the recipe output, split editor with the live liters meter (imports `packLiters`/`meterTone` from BatchTuneDialog so the two surfaces cannot drift), POSTs `plan_type:'base_batch'` — a new request-level variant added the same day in gt-factory-os (contract §6.2b; stored `plan_type` stays `'production'`, no migration; tests C1–C8 8/8). Header "Add batch" takes the no-drafts primary slot and "Add production" drops to secondary permanently — one primary CTA, and the primary now points at the common case. Also lands the audit's three FLOW_COMPLETION findings: the same-day base-batch guard (born from the 2026-07-12 double-production incident) gains **"Tune the batch instead"** via an opt-in `extraLabel` third action on ConfirmDialog (resolves `"extra"`; boolean callers untouched); BatchTuneDialog can **append a member item** to an existing split, constrained to the base head's items exactly as the backend's `PACK_ITEM_WRONG_BASE` validates; the per-item success toast carries **item · qty · date** from `resp.echo` instead of a bare "Production added". Root cause chain recorded in gt-factory-os-production-brain `docs/lessons_learned.md` (the INNER-JOIN reader bug that made batch rows invisible to a schedule query). tsc 0, eslint 0, vitest 1155/1155 (+12).
- `docs/portal-os/tranches/159-schedule-conflict-recovery.md` — built: **scheduling a purchase order works, and the one failure she can clear says so in Hebrew** (Tom 2026-08-02, with a screenshot of the schedule panel showing the bare string `PO_CHANGED_REVIEW_REQUIRED`: "לא עובד לי לתזמן הזמנת רכש. תתקן את זה כי שמתי REVIEW"). Two defects behind one screenshot. **Root cause, backend** (`gt-factory-os` #202, merged `1c00291`, shipped first): the placement-queue read selected `q.updated_at` **raw** — the only timestamp in that `SELECT` not cast to `::text` — and with no `setTypeParser` in the API the pg driver hands back a JS `Date` (millisecond) for a `timestamptz` column (microsecond), so the sub-millisecond digits were gone before the value reached the portal; the portal sent the truncated value back as `expected_updated_at` and `fn_schedule_purchase_order` (migration 0301) compared it `IS DISTINCT FROM` the full-precision column and raised. Any PO whose `updated_at` carried non-zero microseconds — nearly all — **could never be scheduled**; deterministic, not a race, and the concurrency guard was correct all along, it was being fed a lossy value. **Portal, this tranche**: a genuine concurrent edit still hits that guard, and what the office manager saw was the raw enum with no way out. The code reached the screen because these endpoints return `{ error: <CODE>, detail: <raw exception text> }` and the raw text *contains the code*, so `jsonOrThrow` preferring `detail` as the more human field printed the machine code verbatim — now a `REASON_TEXT` map covering every reason code this surface can hit, plus a `looksLikeCode` guard degrading an unrecognised SCREAMING_SNAKE string to the Hebrew fallback, with genuinely human `detail` still passing through. And the conflict was a dead end: the queue is cached (`staleTime` 30s) and FLOW-001 deliberately stopped refetching on window focus so in-flight edits survive, so a row open a few minutes carries an `updated_at` the backend keeps rejecting and every retry 409s forever — `useScheduleOrder` now invalidates the queue on that specific code, the row refreshes under the open panel, and her next save (same date, nothing re-typed) lands. `ApiError` gained an optional `code` for code to branch on, never for display. tsc 0, eslint 0, vitest 1159/1159 (+4: the code is never shown even when the backend puts it in both `error` and `detail` while `.code` still carries it; the queue is refetched on the conflict; an unknown bare code degrades to the fallback; human `detail` still passes through).
- `docs/portal-os/tranches/160-run-report-uom-mismatch.md` — built: **the operator can finish a production run** (Tom 2026-08-06, with a screenshot of `/production/runs/…/report` for REVIVE 1L showing `output_uom UNIT does not match items.sales_uom BOTTLE`: "למה זה לא נותן לי להזין את הייצור הזה?"). `ReportForm` sent `outputUom: data.uom` on submit, but `data.uom` is the **run's** unit — copied at materialization from `production_plan.uom`, a planning-side label that is generically `UNIT` — while the report endpoint books FG output in `items.sales_uom` and 409s `UOM_MISMATCH` on anything else. The portal was echoing a value it has no authority over into a field the backend derives anyway, so the two disagreed for every item not sold in `UNIT`: 34 active manufactured/repack items in `BOTTLE`, 4 `BAG`, 1 `TIN` against 4 in `UNIT` — ~90% of the catalogue, deterministic, and "Try again" retried the identical body. `output_uom` is optional in the contract and already defaults to `items.sales_uom`, so the fix is to stop sending it: the smallest diff and the only value that could ever be correct — the field had no degrees of freedom, only a way to be wrong. Backend companion on the same branch adds a `run.uom` fallback so a nullable `sales_uom` can never post a blank UOM onto the ledger row. **Knowingly left:** the pre-submit display still reads the plan's unit ("Making 180 UNIT") — the count is identical and the success state already reads `output_uom`, so stock is never mis-stated, but the label is inconsistent mid-flow; fixing it means `handleProductionRunToday` resolving `items.sales_uom` for SINGLE runs the way it already does for PACK (`handler.ts:440` vs `:463`), which is backend lane and only affects newly materialized rows. tsc 0, vitest 23/23 on `report.test.ts` (no new case: `buildReportBody` already covers "omits output_uom when none is supplied", which is exactly the path this puts the screen on).
- `docs/portal-os/tranches/161-placement-queue-write-failures.md` — built: **the office manager can place a partial order, and a failed write says what went wrong** (Tom 2026-08-09, three screenshots in one sitting while placing the raw materials for the 18,400-bottle 300ML run: "שינוי מועד ההזמנה נכשל", "זה לא נותן לי לבצע את זה!!!", "תתקן את זה שאוכל לפתוח הזמנת רכש ידנית וזה יעבוד"). Two portal defects plus a backend one, all on the same flow. (1) `line_prices` was built from **every** line and `canPlace` demanded a positive price on **every** line — including a line just marked `לא יסופק`. `fn_place_purchase_order` cancels the unplaced line and then walks `line_prices`, so pricing a line the same call cancels raises `PO_LINE_NOT_OPEN`; partial placement could never succeed. Both sites now ask `isPlacedLine`, never "every line". Reproduced against the live function both ways, rolled back. (2) `REASON_TEXT` mapped 2 of the 11 codes these endpoints emit, so nine failures — `SAFE_DATE_OVERRIDE_REQUIRED`, `SAFE_DATE_OVERRIDE_NOTE_REQUIRED`, `INVALID_SCHEDULE_DATE_*`, `SCHEDULE_PO_FAILED`, `PLACE_ORDER_FAILED`, the `PO_LINE_*` family — all degraded to one generic sentence; and the one schedule key present, `SCHEDULE_DATE_IN_PAST`, is not a code the backend sends (`INVALID_SCHEDULE_DATE_PAST` is), so it never matched either. Same defect the file's own comment records being fixed once for a single code on 2026-07-30, with the siblings left behind. (3) Backend companion `gt-factory-os` migration **0317**: `fn_allocate_po_number` trusted `po_number_seq_per_year` absolutely, so any row inserted out of band left it behind and every allocation returned a taken id → `23505` → unmapped → 503 → "Could not submit" / "הכתיבה מושהית כעת"; live `next_seq = 301` against existing `PO-2026-00301..00306` had killed both manual PO creation and partial placement (which allocates a PO for the remainder). The allocator now verifies the id is free and skips forward, so it is correct however a row got in. **Knowingly left:** `/purchase-orders/new` still shows a generic sentence for 5xx — with 0317 in place the 503 it was masking is gone, and giving that page the same reason map is its own tranche. tsc 0, vitest 26/26 (the pre-existing split-payload assertion was `arrayContaining`, which passed with or without the bug — tightened to exact equality, plus a new case placing a split when the unsupplied line has no price typed).
- `docs/portal-os/tranches/162-sales-workspace.md` — landed (merged 2026-08-18, #213; backend companion #220): **GT Sales Workspace** — `/apps` switchboard + Hebrew-RTL `(sales)` route group (Today queue + one-tap outcome loop, leads table + drawer, orgs, quick-add, global search, scoped PWA, minimal settings) on the live `sales_core` data (188 leads). Authorized by Tom's 2026-08-17 masterprompt (`gt-factory-os-production-brain/docs/plans/2026-08-17-sales-portal-ui-masterprompt.md`) + same-day UX-iteration addenda. Plan: `gt-factory-os-production-brain/docs/superpowers/plans/2026-08-17-sales-workspace-implementation.md`. Backend lane companions: gt-factory-os migrations 0322–0323 + `api/src/sales/**`. Factory scorecard target: zero regression. UX release gate **SHIP** after 3 of a maximum 3 iterations (run 1 HOLD, 4 P0 / 21 P1 → run 2 0 P0 → run 3 0 new P0/P1). vitest 1353/1353, tsc 0, eslint 0, next build green.
- `docs/portal-os/tranches/163-factory-to-sales-switch.md` — built: **the sales workspace is reachable from the operations portal in one tap** (Tom 2026-08-18, minutes after first opening the workspace on production: "צריכה להיות גם אופצייה פשוטה לעבור לצד של המכירות מהצד של התפעול"). 162 shipped the crossing in one direction only — `SalesShell` carries a `מעבר לייצור` control landing on `/home`, added because the phone had no way out of the sales tab bar at all, but nothing went the other way: from the operations portal sales was `/sales/today` typed by hand, or `/apps`, itself only reachable by typing. One control in `TopBar`, mirroring the sales-side one rather than inventing a second pattern — same `ArrowLeftRight` icon, icon-only on phones and labelled from `sm` up, in the `ml-auto` cluster beside the command palette so it is present on every operations route without touching `NAV_MANIFEST` or the sidebar. Goes straight to `/sales/today`, not to `/apps`: the sales-side control goes straight to `/home`, and a switchboard on only one of the two directions is an asymmetry the user has to learn. `role === "admin"` only, the same predicate gating `/apps` and the `RoleGate` on the `(sales)` group, so operator, planner and viewer see the topbar exactly as before. Label stays English — the factory shell is English-first and the `CLAUDE.md` Hebrew exception covers `/apps` and `(sales)` only; the destination is Hebrew, the doorway to it is not. tsc 0, vitest 1358/1358 (+5: admin sees it and it points at `/sales/today`; operator, planner and viewer see nothing; and the accessible name survives the width at which the visible label is hidden — the phone case, where the control is icon-only and would otherwise reach a screen reader unnamed), eslint 0, next build green.
- `docs/portal-os/tranches/164-queue-triage-and-cap.md` — built: **188 leads become a morning you can work** (2026-08-18 v2 audit P0-1/P0-5/P1-3/P1-14; U-011). The Today queue had no shape and no owner: ordering was hardcoded twice — the handler sorted `coalesce(next_touch_at, created_at) asc` over the view's `item_type` grouping — so the first card every morning was the *oldest* row in the system, a 2023 lead nobody can call, and the only cap was `PAGE = 12`, which is render batching, not a commitment: all 188 stayed owed. 39 of those leads carry neither phone nor email and could never be cleared by any outcome. Age was invisible (`fmtRelative` at 12px muted renders "לפני 19 ימים" like "לפני יום") and the SLA badge, meant to be rare, was on **188 of 188** cards. The strip read `0 · 0 · 0`, because its three counts describe steady state and every one is zero while a batch-imported backlog is worked down. Backend companions in `gt-factory-os` 0326 (queue settings, `age_days`/`uncontactable`, the four triage counts); this tranche is the screen: `capRows` caps the two backlog sections at the admin-owned `daily_cap` and states the remainder truthfully (`היום: 15 שיחות · עוד 121 ממתינות בתור`) — deferred, never hidden — while conversions and returning customers are never capped because they are news, not workload; order comes from `app_setting('queue')` with `newest_first` as the default (D3); `agedTone` tints age past the live SLA and the card states `בן 19 ימים` outright; the SLA badge renders only when overdue so red means something again; the strip carries queue / overdue / unowned / never-contacted; and the leads table becomes navigable at 188 rows — mobile list paginates 20 at a time, the age column sorts, and a chip isolates the 39 nobody can call so they stay findable instead of silently dropped.
- `docs/portal-os/tranches/165-outcome-loop-integrity.md` — built: **every call ends captured, wherever it was placed from** (2026-08-18 v2 audit P0-4/P1-1/P1-4/P1-5/P1-6/P1-9/P1-10). `OutcomeSheet` mounted only on `/sales/today`, but the leads drawer armed the very same intent — same `record_outreach`, same sessionStorage handoff — and `today/page.tsx` then *cleared* any pending intent whose lead was not in today's rows. So calling a `working` lead with a future next-touch from the table wrote the outreach event, asked nothing on return, and discarded the answer with no toast and no log: "every call ends with the next one already scheduled" held only for queue members. Four smaller holes on the same loop: the backdrop click ignored the `busy` guard that Escape and the dismiss button both honoured, so a stray tap unmounted the sheet mid-write and the error had nowhere to land; "לא ענה" and "וואטסאפ נשלח" committed a server-chosen date nobody saw (and until 0324 that date was a fixed 06:00 UTC that scheduled Friday and Shabbat calls); the drawer's `<select>` had no free-text branch, so choosing "אחר" stored the word itself as `lost_reason` — the exact failure migration 0318's header says the schema exists to prevent; Escape discarded an unsaved note without asking; and mark-lost had no undo, so recovery meant hunting the lead down and setting the status back by hand. Two error messages also lied: a network failure on any POST reported "לא הצלחנו לטעון את התור", a read error for a write, and an expired session surfaced the API's English "Not authenticated" inside a Hebrew UI. Now both pages mount the sheet and resolve their own row; the today page clears an intent only on a captured outcome or an explicit dismissal, falling back to the leads cache for the name; the backdrop honours `busy`; `nextBusinessTouchPreview` mirrors 0324's rule client-side (09:00 Israel, never Fri/Sat, pinned to the server's answer by test) and shows the date before it is committed, with "שנה תאריך" beside it, while the request still omits the date unless the user changed it so the server stays the source of truth; the drawer's lost path is the sheet's radio group including free text, keyed on the *last* reason rather than the literal "אחר" so Tom renaming it cannot silently kill capture; moving to בטיפול collects the next touch in the same action, which is what 0324 now requires; Escape with unsaved text asks first; mark-lost offers `בטל` for the life of its toast; and a failed write says the write failed, with `AUTH_EXPIRED` added to the proxy's 401 so an expired session can say so in Hebrew. tsc 0, vitest 1374/1374 (+5), playwright sales-outcome-integrity 6/6 plus the three existing sales specs green.
