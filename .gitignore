node_modules
.next
out
dist
.env.local
.env.*.local
*.log
.DS_Store
.vscode
.idea
next-env.d.ts
.next/

# Added before Phase A baseline
test-results/
tsconfig.tsbuildinfo
*.tsbuildinfo
coverage/
playwright-report/

.vercel

# Local design-intelligence tooling (ui-ux-pro-max skill installed via
# `uipro init --ai claude` for design system queries). Skill artefacts
# are local tooling, not portal source — don't ship them.
#
# Exception: the impeccable design skill is vendored deliberately (Apache-2.0,
# tranche 162 / masterprompt 2026-08-17 §3.4) so every session shares one
# pinned version. Contents are excluded with /* rather than the directory
# itself, because git cannot re-include a path whose parent directory is
# excluded.
.claude/skills/*
!.claude/skills/impeccable/
#
# Exception: the apple-design skill (Apple HIG design rules, generalised for
# cross-platform UI) is vendored on the same terms, so portal design reviews
# run against one pinned copy instead of a per-machine install. Upstream
# states no licence; see gt-factory-os-production-brain/.claude/skills/VENDORED.md
# for the provenance ledger covering both copies.
!.claude/skills/apple-design/
#
# Exception: the interface-rules set vendored 2026-08-26 from boraoztunc/skills
# (Tom's direction). The seven `better-*` domain skills plus `interface-review`
# are jakubkrehel/skills, MIT — LICENSE-jakubkrehel and NOTICE-jakubkrehel.md
# travel with them. `web-design-guidelines` and `vercel-react-best-practices`
# come from the same re-publisher. Provenance and routing:
# .claude/skills/VENDORED.md.
!.claude/skills/better-accessibility/
!.claude/skills/better-colors/
!.claude/skills/better-interface/
!.claude/skills/better-layout/
!.claude/skills/better-typography/
!.claude/skills/better-ui/
!.claude/skills/better-writing/
!.claude/skills/interface-review/
!.claude/skills/web-design-guidelines/
!.claude/skills/vercel-react-best-practices/
!.claude/skills/LICENSE-jakubkrehel
!.claude/skills/NOTICE-jakubkrehel.md
!.claude/skills/VENDORED.md
design-system/
