node_modules
.next
out
dist
.env.local
.env.*.local
*.log
.DS_Store
.vscode
.idea
next-env.d.ts
.next/

# Added before Phase A baseline
test-results/
tsconfig.tsbuildinfo
*.tsbuildinfo
coverage/
playwright-report/

.vercel

# Local design-intelligence tooling (ui-ux-pro-max skill installed via
# `uipro init --ai claude` for design system queries). Skill artefacts
# are local tooling, not portal source — don't ship them.
.claude/skills/
design-system/

# /graphify knowledge-graph analysis output — local tooling, not portal source.
graphify-out/
