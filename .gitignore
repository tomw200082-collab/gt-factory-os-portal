node_modules
.next
out
dist
.env.local
.env.*.local
*.log
.DS_Store
.vscode
.idea
next-env.d.ts
.next/

# Added before Phase A baseline
test-results/
tsconfig.tsbuildinfo
*.tsbuildinfo
coverage/
playwright-report/

.vercel

# Claude skills are shipped (cavekit spec-driven-dev + ui-ux-pro-max design
# tooling) so they're available in every web/remote session, not just locally.
# `design-system/` below stays local — it's generated query output, not source.
design-system/
